# سامانه تغییرات موتور استور انجین UNFINIT (UNFINIT Store Engine Changelog)

تمام تغییرات کلیدی، هات‌فیکس‌ها و ارتقاءهای معماری پروژه در این سند ثبت می‌گردد.
فرمت این سند بر پایه استانداردهای [Keep a Changelog](https://keepachangelog.com/fa/1.0.0/) و نسخه‌گذاری معنایی تدوین شده است.

## [v0.7.8] - 1403/07/15

### 🎛 تنظیم همزمان سقف بله و درصد بافر در وب‌پنل، اسپلیت خودکار پیش‌پرواز و تفکیک نام‌گذاری
- **پیکربندی داینامیک سقف و بافر امنیتی بله (`get_bale_compression_settings`):** افزودن فیلد درصد بافر امنیتی انکودر (`bale_safety_buffer_percent` با پیش‌فرض ۳.۰٪) در کنار سقف مجاز (`bale_max_file_size_mb` با پیش‌فرض ۵۰MB) در کارت پیام‌رسان بله و مودال اختصاصی وب‌پنل، با محاسبه خودکار و بی‌صدای حجم هدف مؤثر انکودر (`effective_target = base_cap * (1.0 - (buffer_percent / 100.0))`).
- **اسپلیت خودکار پیش‌پرواز در ارسال گروهی (Batch Pre-flight Auto-Split):** شناسایی هوشمند ویدیوهای با مدت زمان بالای ۲۵ دقیقه (۱۵۰۰ ثانیه) یا حجم بالای ۱۲۰MB پیش از شروع انکود در پردازش دسته‌ای (`process_batch_sequentially`)، و تقسیم خودکار آن‌ها به پارت‌های ۲۵ دقیقه‌ای استاندارد با `SmartVideoSplitter.split_video_async` بدون افت کیفیت و ارسال تمام پارت‌ها با `is_split_part=True`.
- **تفکیک قطعی نام‌گذاری گروهی از اسپلیت:** حفظ نام پاک و تمیز بدون برچسب پارت برای تمام فایل‌های مستقل در ارسال‌های چندفایله، و اختصاص برچسب پارت صرفاً به فایل‌هایی که عملاً تقسیم شده‌اند.
- **تثبیت نمایش حجم هدف در تلگرام:** نمایش سقف پایه کاربر (`🎯 حجم هدف: زیر {raw_cap_mb:.1f} مگابایت`) در نوار پیشرفت فشرده‌سازی تلگرام جهت شفافیت و آرامش خاطر کاربر، در حالی که انکودر با سقف مؤثر مارجین‌دار عمل می‌کند.
- **ثبت قوانین مهندسی ۵.۱۸ و ۵.۱۹ در منشور `AGENTS.md`:** ماندگارسازی قوانین تنظیم سقف و بافر و اسپلیت خودکار پیش‌پرواز.

## [v0.7.6] - 1403/07/15

### ⚡️ تفکیک قطعی نام‌گذاری گروهی از اسپلیت، سرعت انکودینگ ۶x+ و ایزولاسیون متادیتا
- **تفکیک قطعی نام‌گذاری ارسال گروهی از اسپلیت (`clean_public_filename`):** تفکیک پارامتر `is_split_part`؛ برچسب «پارت X از Y» صرفاً و منحصراً به ویدیوهای تقسیم‌شده با ابزار Split تعلق می‌گیرد (`is_split_part=True`). فایل‌های مستقل ارسال‌شده در دسته‌های گروهی (Batch Forwarding / Queue) نام تمیز و بدون برچسب پارت خود را حفظ می‌کنند (مانند `🎬 1-jabe-laitner.mp4`).
- **بازیابی سرعت فشرده‌سازی ۶ برابر و بالاتر با FFmpeg:** ارتقای پارامترهای فشرده‌سازی در تمام ماژول‌ها به `-preset veryfast -tune fastdecode -threads 0` و فیلتر مقیاس هوشمند `-vf "scale='min(1280,iw)':-2"` جهت بهینه‌سازی حداکثری سرعت پردازش در سرور بدون افت کیفیت بصری.
- **ایزولاسیون متن و رفع به‌هم‌ریختگی RTL در نوار پیشرفت فشرده‌سازی:** تفکیک قطعی حجم اولیه و حجم هدف به دو سطر مجزا بدون استفاده از کاراکترهای فلش (`➔`)، جهت نمایش کاملاً صحیح اعداد در کلاینت‌های راست‌به‌چپ تلگرام.
- **پاکسازی متادیتای ساختگی در ارسال‌های صوتی:** برچیدن کامل انتساب فرضی نام خواننده (`performer="UNFINIT"`) در تلگرام و بله و پالایش تگ‌های صوتی جهت نمایش تمیز و واقعی در موزیک‌پلیر بومی بله.
- **ثبت قوانین ۵.۱۴ تا ۵.۱۷ در منشور مهندسی `AGENTS.md`:** تثبیت درس‌های آموخته‌شده به عنوان اصول دائمی معماری نرم‌افزار.

## [v0.7.4] - 1403/07/15

### 🗜️ پویایی کامل سقف حجم بله، فشرده‌سازی غیرمسدودکننده و نوار پیشرفت یکپارچه
- **پویایی کامل سقف حجم بله (`get_bale_max_size_mb`):** برچیدن کامل تمامی اعداد و مقادیر هاردکد (۴۸.۰، ۴۸.۵ و ۵۰ مگابایت) و استعلام خودکار سقف از تنظیمات وب‌پنل در `/data/settings.json` با فال‌بک امن.
- **فشرده‌سازی غیرمسدودکننده آسنکرون (`compress_video_async`):** اجرای فرآیند بهینه‌سازی ویدیویی FFmpeg به صورت کاملاً ناهمگام با `asyncio.create_subprocess_exec` و فلگ‌های `-progress pipe:1` و `-threads 0` بدون کوچک‌ترین قفل شدن در حلقه رویدادهای Asyncio.
- **نوار پیشرفت یکپارچه و زنده تلگرام:** اتصال جریان پیشرفت فشرده‌سازی به پیام تلگرام با تراتل زمانی ۳.۰ ثانیه و قالب بلاکی استاندارد `[████░░░░░░]` همراه با نمایش سرعت، زمان باقیمانده و حجم هدف.
- **ارسال مستقیم ویدیوی فشرده به بله با نام پاک:** تحویل فوری ویدیوی بهینه‌شده به بله از طریق `send_video` نیتیو با نام تمیز و بدون هش (`clean_public_filename`) و ایموجی 🎬.

## [v0.7.3] - 1403/07/15

### 🎧 پخش بومی فایل‌های صوتی، پاکسازی نام‌ها و استانداردسازی ایموجی‌های رسانه‌ای
- **پخش بومی موزیک در پلیر بله با `sendAudio`:** فایل‌های صوتی به جای سند عمومی، با متد `/sendAudio`، نوع داده `audio/mpeg` یا `audio/mp4` و متادیتای خواننده و قطعه ارسال می‌شوند تا مستقیماً در پلیر اختصاصی با اکولایزر بله پخش گردند.
- **استریم پرسرعت `ThrottledFileStreamer` در `send_audio`:** انطباق ارسال فایل صوتی با پایپ‌لاین چانک‌های ۲۵۶KB و تایم‌اوت ۳۰۰ ثانیه‌ای با `sock_read=None` جهت ارسال سریع و بدون افت سرعت.
- **پاکسازی قطعی نام‌ها و پارت‌ها با `clean_public_filename`:** پالایش کامل نام فایل‌ها و کپشن‌ها از پیشوندهای پردازشی و هش‌های هگز (`compressed_`, `8a4be8db_`) و درج نام‌گذاری استاندارد و زیبای فارسی برای پارت‌های اسپلیت (`[نام اصلی] - پارت ۱ از ۲.mp4`).
- **استانداردسازی ایموجی‌های سه‌گانه رسانه‌ای:** تفکیک قطعی آیکون‌های رسانه‌ای: `🎬` برای ویدیو، `🎧` برای صوت و موسیقی، و `📄` برای اسناد و مدارک.
- **نوار پیشرفت چشم‌نواز فشرده‌سازی ویدیو (`format_compression_progress`):** نمایش نوار مدرن بلاکی درصد، سرعت بهینه‌سازی، زمان باقیمانده و مقایسه حجم اولیه با حجم هدف تخمینی.
- **بهینه‌سازی تنظیمات FFmpeg:** ارتقای پارامترهای پردازش به `-preset faster -crf 23 -threads 0 -c:a copy -movflags +faststart` جهت حفظ کامل کیفیت صدا بدون بازانکود غیرضروری و آماده‌سازی فایل برای وب.

## [v0.7.2] - 1403/07/15

### 🎬 فعال‌سازی پخش بومی ویدیو در پلیر بله (Native Video Player) با استریم فوق‌سریع
- **ارسال مستقیم ویدیو از طریق `send_video` با پایپ‌لاین چانک‌های ۲۵۶KB:** ارسال ویدیوها به عنوان ویدیوی نیتیو بله همراه با پوستر ویدیویی، ابعاد تصویر (`width` / `height`)، مدت زمان (`duration`) و دکمه Play جهت پخش مستقیم درون‌برنامه‌ای در کلاینت‌های بله.
- **تطبیق استریمر پرسرعت `ThrottledFileStreamer` در `send_video`:** استفاده از استریم آسنکرون ۲۵۶ کیلوبایتی با `quote_fields=False` و `sock_read=None` جهت دستیابی به زمان انتقال ۵ ثانیه‌ای برای فایل‌های ۲۶ مگابایتی.
- **فال‌بک خودکار تک‌مرحله‌ای به `send_document`:** در صورتی که سرور بله به هر دلیل با پردازش ویدیویی موافقت نکرد، بلافاصله فایل بدون فوت وقت به عنوان سند تحویل داده می‌شود.
- **پیام موفقیت تفکیک‌شده در تلگرام:** نمایش پیام جذاب «🎬 ویدیوی تصویری با موفقیت به بله منتقل شد و آماده پخش است!» پس از تحویل موفق فایل ویدیویی.

## [v0.7.1] - 1403/07/15

### 🚀 تفکیک داینامیک مقصد بله، ارسال سریع بدون ترنسکودر و نوار پیشرفت نرم
- **تفکیک و اولویت‌بندی کاملاً پویا برای چت مقصد بله (`get_bale_target_chat`):** بازنشانی اولویت‌ها به ترتیب: تنظیمات دیتابیس (`bale_channel_id`) ➔ شناسه اکتیو ادمین بله (`get_admin_chat_id`) ➔ کانال یا چت پیش‌فرض در متغیرهای محیطی ➔ مالک بله در کانفیگ. حذف قطعی هرگونه هاردکد استاتیک `402479514` در تمام بخش‌های سیستم.
- **ارسال مستقیم و فوق‌سریع از طریق `send_document`:** دور زدن کامل صف طولانی و قفل‌شونده ترنسکودر ویدیویی بله (`/sendVideo`) که فایل‌های ۲۶ مگابایتی را به مدت ۴۵۰ ثانیه در وضعیت هانگ نگه می‌داشت؛ تحویل آنی فایل‌ها با متد `/sendDocument` بدون کوچک‌ترین تأخیر سرور.
- **استریم چانک‌های ۲۵۶KB با پایشگر پیشرفت تراتل‌شده (`ThrottledFileStreamer`):** ارسال غیرهمگام بایت‌ها با بالاترین ظرفیت پهنای باند شبکه و به‌روزرسانی نوار پیشرفت در تلگرام حداکثر هر ۴ ثانیه یک‌بار جهت محافظت کامل سوکت از فلویدلیمیت‌های تلگرام و انجماد شبکه.
- **بهینه‌سازی تایم‌اوت‌های سوکت:** ارتقاء سقف ایمن تایم‌اوت سوکت به `total=600s, connect=30s, sock_read=300s` جهت عبور موفق فایل‌های سنگین در هر شرایط ترافیکی.

## [v0.7.0] - 1403/07/15

### 🚀 بازگردانی قطعی معماری اثبات‌شده و پایدار v0.5.8 در خط لوله ارسال بله و حذف هانگ سوکت
- **بازگشت به استریم بومی و سبک سیستم‌عامل (`with open(p, "rb") as f`):** لغو فراخوانی‌های پرهزینه بارگذاری بایت‌ها در رم (`read_bytes()`) و بازگشت به استریم استاندارد و آزمایش‌شده سیستم‌عامل که در نسخه‌های v0.5.6 تا v0.5.8 عملکرد درخشان و سریعی داشت.
- **تثبیت ساختار فرم چندبخشی (`quote_fields=False`):** بازگردانی تنظیم دقیق هدرها و فیلدهای چندبخشی aiohttp مطابق نسخه اثبات‌شده v0.5.8 که سازگاری کامل با انجین Nginx پیام‌رسان بله دارد.
- **تنظیم تایم‌اوت متعادل و بهینه (`total=450s, connect=30s, sock_read=180s`):** ایجاد فرصت کافی برای آپلود فایل‌های ۲۶ مگابایتی و بالاتر حتی در شرایط نوسان شبکه بدون قفل شدن طولانی‌مدت سرور.
- **برچیدن لوپ‌ها و فال‌بک‌های تودرتوی اورمهندسی:** حذف حلقه‌های تکراری تلاش مجدد و فال‌بک‌های مضاعف در تلگرام و بله که در صورت بروز خطا موجب معطلی ۱۵ دقیقه‌ای ربات می‌شد؛ جایگزینی با زنجیره فال‌بک تک‌مرحله‌ای شفاف و تمیز.
- **اصلاح ریشه‌ای گزارش خطا در تلگرام و رفع نمایش `None`:** تصحیح استخراج متن خطا با بررسی چندگانه `res.get("description") or res.get("error") or str(res)` جهت انعکاس واضح و دقیق پاسخ سرور بله به جای پیام گنگ `None`.

## [v0.6.10] - 1403/07/15

### ⏱ تمدید تایم‌اوت سوکت شبکه بله به ۳۰۰ ثانیه و تثبیت نهایی خط لوله ارسال
- **تمدید سخاوتمندانه تایم‌اوت سوکت (`total=600s, connect=30s, sock_read=300s`):** بر پایه لاگ‌های ابری هاگینگ‌فیس، آپلود فایل‌های ۲۶ مگابایتی به سرورهای بله در ایران بین ۱۱۰ تا ۱۴۰ ثانیه زمان می‌برد که تایم‌اوت سختگیرانه ۹۰ ثانیه‌ای در ۸۰٪ مسیر منجر به `SocketTimeoutError` می‌شد. با ارتقاء `sock_read` به ۳۰۰ ثانیه و کل به ۶۰۰ ثانیه، انتقال امن حتی در زمان افت سرعت شبکه تضمین گردید.
- **اعمال یکپارچه در تمام متدها (`send_video`, `send_document`, `send_audio`):** اعمال تنظیمات جدید هم در فراخوانی اولیه و هم در تمامی گام‌های فال‌بک خودکار.
- **به‌روزرسانی پیام وضعیت تلگرام به پنجره زمانی ۲ دقیقه‌ای:** نمایش پیام شفاف `🚀 در حال انتقال پرسرعت فایل به بله (XX.X مگابایت)... \n⏳ با توجه به حجم فایل، این فرآیند حدود ۲ دقیقه زمان میبرد. لطفاً صبور باشید.` و ثبت نام فایل پس از موفقیت.

## [v0.6.9] - 1403/07/15

### 🚀 رفع قطعی هانگ سوکت آپلود بله با ارسال مستقیم بایت‌های خالص (Direct Bytes Dispatch)
- **ارسال مستقیم بایت‌های خالص در حافظه (`read_bytes()`):** خواندن مستقیم فایل‌ها به صورت بایت در حافظه رم و تحویل به `aiohttp` به جای ارسال فایل‌پوینتر باز؛ این کار سبب ارسال خودکار و قطعی هدر استاندارد `Content-Length` توسط کلاینت شده و مانع اجباری شدن فرمت تکه‌ای (`Transfer-Encoding: chunked`) که توسط وب‌سرور Nginx بله معلق می‌ماند، گردید.
- **فعال‌سازی استاندارد RFC در بدنه مالتی‌پارت (`quote_fields=True`):** تضمین سازگاری کامل نام و فیلدهای ارسالی با پروتکل HTTP در وب‌سرور پیام‌رسان بله.
- **کاهش تایم‌اوت‌های سوکت شبکه به مقادیر منطقی:** تغییر تایم‌اوت کلی از ۹۰۰ ثانیه به ۱۸۰ ثانیه (`connect=20s`, `sock_read=90s`) جهت جلوگیری از انجماد طولانی‌مدت موتور در صورت اختلالات گذرای شبکه.
- **فال‌بک خودکار و بلادرنگ به `send_document`:** در صورتی که ارسال از طریق `send_video` یا `send_audio` با عدم تایید بله مواجه شود، بلافاصله و با همان بایت‌های حافظه فالبک به `send_document` اجرا می‌گردد.
- **پیام وضعیت تک‌ادیتی در تلگرام با نمایش دقیق حجم فایل و خطاهای تفصیلی:** ثبت حجم مگابایت در متن پیام تلگرام و انعکاس شفاف کدهای خطا در صورت عدم موفقیت.

## [v0.6.8] - 1403/07/15

### ⚡️ بازیابی قطعی سرعت حداکثری آپلود بله (High-Speed Direct Bale Dispatch Pipeline)
- **استریم خطی بدون بافر و با بالاترین پهنای باند شبکه (`open(p, 'rb')`):** ارسال فایل‌ها مستقیماً با فراخوانی متدهای `send_video`، `send_audio` و `send_document` بله از طریق هندل مستقیم سیستم‌عامل پایتون بدون هیچ‌گونه رپِر، حلقه محاسبه تکه‌ای (Chunks) یا تاخیر مصنوعی انجام می‌شود. سیستم به صورت خودکار هدر `Content-Length` را ارسال کرده و حداکثر پهنای باند را در دسترس قرار می‌دهد.
- **زنجیره فال‌بک ضدگلوله استخراج شناسه چت مقصد بله (`get_bale_target_chat`):** پیاده‌سازی متد اختصاصی با اولویت‌بندی هوشمند پنج‌گانه: `BALE_CHANNEL_ID` پیکربندی ➔ `DEFAULT_BALE_CHAT_ID` ➔ تنظیمات سیستم در دیتابیس (`bale_channel_id`) ➔ متد `get_admin_chat_id()` آداپتور بله ➔ `BALE_OWNER_ID`. در نتیجه هرگونه مشکل ناشی از عدم تنظیم چت ادمین یا عدم ارسال پیام قبلی برطرف گردید.
- **پشتیبانی رسمی از `BALE_CHANNEL_ID` و `DEFAULT_BALE_CHAT_ID` در هسته (`core/config.py`):** تعریف متغیرهای محیطی با بارگذاری پویا و خوانش اولویت‌دار جهت هدایت مستقیم فایل‌ها به کانال‌ها و گروه‌های اختصاصی بله.
- **پیام وضعیت شیک، تک‌ادیتی و غیرمسدودکننده در تلگرام (`platforms/telegram_adapter.py`):** حذف کامل آپدیت‌های پی‌درپی و ویرایش‌های مکرر تلگرام که باعث مسدودی سوکت و ریت‌لیمیت می‌شد؛ پیام با فرمت پرسرعت نمایش داده شده و پس از پایان انتقال بلافاصله پیام موفقیت‌آمیز همراه با نام فایل را ثبت می‌کند.
- **لاگ‌های تشخیصی سطح شبکه (`[Bale Dispatch]`, `[Bale Response]`, `[Bale Error]`):** ثبت دقیق اندازه فایل بر حسب مگابایت، شناسه مقصد، وضعیت HTTP سرور بله و محتوای پاسخ در وب‌پنل و کنسول جهت پایش لحظه‌ای و دیباگ هوشمند.

## [v0.6.7] - 1403/07/15

### 🤖 تثبیت و همگام‌سازی فوری دیسپچر تلگرام و احیای پاسخ‌گویی `/start` (`platforms/telegram_adapter.py`)
- **همگام‌سازی فوری و سنکرون دیسپچر پایروگرام (`_sync_add_handler`):** در نسخه پیشین، ثبت هندلرهای پایروگرام در یک تسک ناهمگام روی لوپ زمان‌بندی می‌شد که در صورت بار اول یا عدم اجرای تیک لوپ، دیسپچر در لحظه استارت خالی می‌ماند. با تزریق متد سنکرون، تمام ۷۴ هندلر (شامل لاگر سراسری، دستور `/start`، دکمه‌های شیشه‌ای و مدیا) در همان میلی‌ثانیه‌های نخستین مستقیماً در دیکشنری `self.app.dispatcher.groups` ثبت و فعال می‌شوند.
- **ثبت تضمینی هندلرها درون `start_client` همراه با لاگ رسمی:** فراخوانی `self.register_handlers()` به درون متد `start_client` افزوده شد تا در صورت اتصال مجدد یا ریست سشن، کلیه هندلرها با لاگ `[TG MTProto] All 74 handlers registered successfully across 2 groups!` تضمین و اعتبارسنجی شوند.
- **تضمین سلامت کامل تست‌های ایزوله و زمان اجرا:** اجرای موفقیت‌آمیز آزمون صحت ثبت هندلرها بدون هیچ‌گونه خطا یا استثنای زمان اجرا.

## [v0.6.6] - 1403/07/15

### 🚀 بازگشت به استریم خالص بومی سیستم‌عامل و حذف موانع سرعت (`platforms/bale_adapter.py`)
- **استریم مستقیم و خالص بومی پایتون (`open(file, 'rb')`):** حذف کامل رپِرهای رم و پیشرفت (`FastUploadStream`) و جریان مستقیم بایت‌ها توسط کرنل لینوکس روی سوکت شبکه با حداکثر توان خطی دیتاسنتر (Wire-Speed).
- **حذف کامل سقف مصنوعی ۲۰ مگابایت:** تمام فایل‌های صوتی تا ۵۰ مگابایت مستقیماً با متد `sendAudio` به همراه تگ‌های `title`، `performer`، `duration`، و `caption` ارسال می‌شوند تا درون موزیک‌پلیر داخلی پیام‌رسان بله به راحتی پخش گردند.
- **ارسال مستقیم تمام ویدیوها با `sendVideo`:** ویدیوها مستقیماً با متد نیتیو ویدیویی ارسال شده و فالبک به `sendDocument` صرفاً در صورت خطای واقعی API بله انجام می‌گردد.
- **تایم‌اوت سوکت استاندارد و مطمئن:** `aiohttp.ClientTimeout(total=1800, connect=60, sock_read=300)`.

### 💬 پیام وضعیت ساده و بی‌سربار در تلگرام (`platforms/telegram_adapter.py`)
- **حذف کامل کالبک‌ها و ادیت‌های مکرر تلگرام:** جلوگیری از ایجاد هرگونه تداخل یا استال سوکت در حین انتقال.
- **نمایش وضعیت ثابت و شیک در حین ارسال:**
  `🚢 در حال ارسال فایل به بله...`
  `⏳ لطفاً شکیبا باشید (فایل‌های حجیم معمولاً ۱ تا ۲ دقیقه زمان می‌برند)`
- **به‌روزرسانی تمیز پس از پایان کار:**
  `✅ فایل با موفقیت به بله ارسال شد.` به همراه هندلینگ کامل پیام خطا در صورت بروز مشکل.

## [v0.6.5] - 1403/07/15

### 🚀 آپلود بله با سرعت حداکثری از طریق استریم رم‌محور بدون قفل (`platforms/bale_adapter.py`)
- **کلاس `FastUploadStream(io.BytesIO)` — Zero-Contention In-Memory Streaming:** کل فایل‌های زیر ۶۰MB با `Path(file_path).read_bytes()` ظرف ۰.۰۱ ثانیه به رم خوانده شده و از طریق این کلاس با چانک ۲۵۶KB به `aiohttp` تحویل داده می‌شوند. این روش هیچ `f.tell()` یا Lock روی هندل دیسک ایجاد نمی‌کند و از Kernel File Pointer Lock Contention که در `v0.6.3` عامل کاهش سرعت از ۴۰۰ KB/s به ۱۲ KB/s بود، به‌طور کامل جلوگیری می‌کند.
- **ثبت `FastUploadStream` به عنوان `BytesIOPayload` در `aiohttp`:** با `pld.PAYLOAD_REGISTRY.register(...)` انجام شده تا `aiohttp` استریم را به صورت نیتیو BytesIO بشناسد و از بافر بهینه استفاده کند.
- **پارامتر `progress_callback` با امضای جدید `(uploaded_bytes, total_bytes, speed_str)`:** نوار پیشرفت درون‌خطی بدون تسک پس‌زمینه — درون حلقه `read()` هر ۲۵۶KB یک‌بار فراخوانی می‌شود.
- **فالبک هوشمند بدون `f.seek(0)`:** در `send_video`، وقتی `sendVideo` به خطا بخورد، `FastUploadStream` جدیدی از همان `ram_data` ساخته می‌شود (بدون نیاز به بازگشت به ابتدای فایل).

### ⚡️ `make_progress_callback` — کارخانه کالبک Thread-Safe (`platforms/telegram_adapter.py`)
- **جایگزینی `track_upload_progress` با `make_progress_callback`:** به جای تسک پس‌زمینه مبتنی بر `f.tell()`، یک کالبک thread-safe با `asyncio.run_coroutine_threadsafe` ساخته می‌شود که مستقیماً از لوپ رویداد اصلی `edit_message_text` را فراخوانی می‌کند.
- **`track_upload_progress` و `run_bale_upload_with_progress` به استاب‌های سازگاری معکوس تبدیل شدند:** هیچ `NameError` یا شکست سازگاری رخ نمی‌دهد.
- **به‌روزرسانی فلوی `split_and_transfer_video_to_bale`:** استفاده از `progress_callback=progress_cb` در `bale_adapter.send_video` به جای `run_bale_upload_with_progress` با `with open(f) as f:`.
- **به‌روزرسانی فلوی `smeta:send_bale`:** ارسال مستقیم `p_final` (مسیر فایل) به `bale_adapter.send_video / send_audio` به جای باز کردن `with open()` در `telegram_adapter`.

## [v0.6.4] - 1403/07/14

### 🛡️ برچیدن قطعی متد مخرب clear_webhook و حفاظت از سکرت‌ها (`platforms/telegram_adapter.py`)
- **حذف ۱۰۰٪ متد `clear_webhook` و هرگونه درخواست `deleteWebhook`:** حذف قطعی درخواست‌های زائد HTTP به `api.telegram.org` که در هاگینگ‌فیس تایم‌اوت می‌خوردند و حلقه رویداد را معطل می‌کردند. کلاینت تلگرام MTProto پایروگرام نیازی به وب‌هوک ندارد.
- **جلوگیری قطعی از نشت توکن در لاگ سرور:** برچیده شدن کلیه URLهای دربردارنده توکن ربات از لاگ‌های عمومی هاگینگ‌فیس.

### ⚡️ رفع فریز هندلر رسانه و پایداری کامل پاسخ‌گویی تلگرام (`platforms/telegram_adapter.py`)
- **محافظت سراسری هندلر رسانه با `try...except`:** قرار گرفتن کل بدنه متد `incoming_media` درون بلوک `try...except Exception as e: logger.exception(...)` به همراه استخراج ایمن شناسه کاربر (`message.from_user.id` با پشتیبانی از پیام‌های فورواردی بدون user) تا هرگز ورکر MTProto مسدود نشود.
- **پولر مستقل و ضدگلوله `track_upload_progress`:** پشتیبانی منعطف از هر دو امضای فراخوانی پیام وضعیت (`status_msg` با متد `edit_text` یا `chat_id` و `status_msg_id` از طریق `self.app`) به همراه مهار کامل استثناها و لغو قطعی تسک در `finally:`.
- **مهار خطای `no running event loop` در `register_handlers`:** گارد امن برای زمان‌هایی که ثبت هندلرها بدون شروع حلقه رویداد (مانند تست‌های ایزوله یا لود اول برنامه) فراخوانی می‌شود.

## [v0.6.3] - 1403/07/14

### 🚀 بازیابی قطعی سرعت حداکثری آپلود بله (Native Wire-Speed Streaming) (`platforms/bale_adapter.py`)
- **حذف کامل رپِرهای کاهنده سرعت و استفاده مستقیم از هندل فایل پایتون (`open(..., "rb")`):** حذف کلاس‌های میانی و تزریق مستقیم فایل‌های خام به `aiohttp.FormData`؛ با این تغییر، بافرینگ کرنل سیستم‌عامل (`sendfile` و TCP Window Scaling) با تمام قدرت فعال شده و سرعت آپلود سرورهای خارجی به سرورهای بله از ۱۲ کیلوبایت بر ثانیه به سرعت واقعی پهنای باند شبکه (~500 KB/s و بالاتر) بازگشت.
- **پایشگر پیشرفت کاملاً مستقل و غیرمسدودکننده با `f.tell()` (`platforms/telegram_adapter.py`):** استعلام آفست جاری نشانگر فایل از طریق متد نیتیو و بدون سربار `f.tell()` هر ۳ ثانیه یک‌بار در تسک پس‌زمینه تلگرام؛ در این روش، سوکت شبکه به طور ۱۰۰٪ خالص و بدون هرگونه وقفه یا اینترپت داده‌ها را ارسال می‌کند.
- **تضمین پایداری تایم‌اوت سوکت (`sock_read=300, total=1800`):** حفظ تایم‌اوت ۳۰ دقیقه‌ای بدون ریسک قطعی زودهنگام در صورت نوسانات موقت اینترنت سرور.
- **هدایت خودکار و امن فایل‌های صوتی سنگین بالای ۲۰MB به `sendDocument`:** جلوگیری از رد شدن فایل‌های صوتی حجیم توسط API متد `sendAudio` بله و ارسال تضمینی آن‌ها به عنوان سند.
- **نمایشگر هوشمند واحد سرعت و درصد زنده در تلگرام:** نمایش واحد بر حسب `KB/s` در سرعت‌های زیر ۱ مگابایت و `MB/s` در سرعت‌های بالاتر برای ارائه خواناترین تجربه کاربری.

## [v0.6.2] - 1403/07/14

### 🤖 رفع قطعی عدم پاسخ‌گویی ربات تلگرام و پاکسازی وب‌هوک (`platforms/telegram_adapter.py`)
- **پاکسازی خودکار وب‌هوک با `clear_webhook`:** ارسال درخواست `deleteWebhook` به تلگرام پیش از آغاز به کار کلاینت MTProto پایروگرام تا در صورتی که وب‌هوکی در تنظیمات توکن فعال مانده باشد، آپدیت‌های تلگرام آزاد شده و به کلاینت MTProto تحویل داده شوند.
- **اعتبارسنجی زنده هویت ربات با `get_me()`:** استعلام شناسه و یوزرنیم ربات پس از اتصال و ثبت در لاگ سیستم (`Verified bot identity: @username (ID: id)`) جهت اطمینان از صحت اعتبارسنجی توکن.
- **ثبت لاگ سراسری پیام‌ها و کال‌بک‌ها در گروه مستقل ۱- (`group=-1`):** تعریف لاگر ورودی مستقل برای تمام پیام‌ها و کال‌بک‌های تلگرام بدون مسدود کردن یا اثرگذاری روی سایر هندلرها جهت دیباگ بلادرنگ ترافیک ورودی.
- **خودترمیمی سشن‌های خراب:** بازنشانی خودکار فایل سشن محلی در صورت بروز خطا در راه‌اندازی اولیه و تلاش مجدد برای اتصال.
- **تزریق سراسری حلقه رویداد (`asyncio.set_event_loop`) در Python 3.12+:** مهار خطای کرش `RuntimeError: There is no current event loop in thread 'MainThread'` هنگام ایمپورت پایروگرام.

## [v0.6.1] - 1403/07/14

### ⚡️ بازیابی سرعت استریم بدون سربار بله با BufferedReaderPayload (`platforms/bale_adapter.py`)
- **رجیستری نیتیو BufferedReaderPayload:** جایگزینی رجیستری کند IOBasePayload با BufferedReaderPayload نیتیو برای کلاس `ProgressFileWrapper`؛ در نتیجه خوانش فایل مستقیماً در بافر سیستم‌عامل و سوکت شبکه ریخته شده و هاپینگ‌های مکرر `run_in_executor` در حلقه آپلود ریشه‌کن گردید (دستیابی به حداکثر پهنای باند و سرعت Wire-Speed).
- **حذف سربار خوانش درون read():** متد `read()` صرفاً شمارنده‌های حافظه‌ای بایت‌ها را به‌روز کرده و هیچ کالبک یا عملیات مسدودکننده‌ای درون چرخه سوکت اجرا نمی‌شود.

### ⏱️ پایشگر کاملاً مستقل پس‌زمینه (Decoupled Background Poller) (`platforms/telegram_adapter.py`)
- **معماری مستقل پایش با `run_bale_upload_with_progress`:** اجرای کوروتین آپلود بله در پس‌زمینه و پایش دوره‌ای هر ۳.۵ ثانیه توسط تسک مجزا؛ اختلالات شبکه تلگرام یا خطاهای ویرایش پیام هیچ تاثیری بر انتقال سوکت بله نخواهد داشت.
- **نمایش هوشمند واحد سرعت (Smart Speed Units):** اگر سرعت انتقال کمتر از ۱ مگابایت بر ثانیه باشد، واحد سرعت به صورت `KB/s` و در صورت افزایش به صورت `MB/s` نمایش داده می‌شود و از ثبت سرعت گمراه‌کننده `0.0 MB/s` جلوگیری می‌گردد.

### 🛡️ هدایت خودکار فایل‌های صوتی حجیم و بهینه‌سازی تایم‌اوت سوکت (`platforms/bale_adapter.py`)
- **هدایت خودکار فایل‌های صوتی بالای ۲۰ مگابایت:** برای فایل‌های صوتی با حجم بالاتر از ۲۰ مگابایت، متد `send_audio` فایل را مستقیماً از طریق متد امن `sendDocument` ارسال کرده تا از خطای ۴۱۳ سرورهای بله جلوگیری شود.
- **افزایش تایم‌اوت سوکت به ۶۰۰ ثانیه:** تنظیم `sock_read=600` در تایم‌اوت ۱۸۰۰ ثانیه‌ای برای آپلودهای حجیم تا در شرایط نوسان شبکه یا اینترنت با تاخیر، اتصال قطع نشود.

## [v0.6.0] - 1403/07/13

### 🚀 حذف تایم‌اوت مخرب ۳۰۰ ثانیه‌ای و احیای آپلود پایدار بله (`platforms/bale_adapter.py`)
- **حذف قطعی قطع ارتباط در ۳۰۰ ثانیه:** جایگزینی تایم‌اوت محدودکننده ۳۰۰ ثانیه‌ای با تایم‌اوت سخاوتمندانه ۱۸۰۰ ثانیه‌ای (۳۰ دقیقه کل، ۶۰ ثانیه اتصال، ۱۸۰ ثانیه خواندن سوکت):
  `timeout = aiohttp.ClientTimeout(total=1800, connect=60, sock_read=180)`
  جهت تضمین پایان موفقیت‌آمیز آپلود فایل‌های حجیم ۴۰ تا ۵۰ مگابایتی (ویدیوهای اسپلیت‌شده و فایل‌های صوتی باکیفیت) بدون قطعی در میانه راه.

### 🌐 استریم استاندارد ۶۴KB و محاسبه خودکار Content-Length (`platforms/bale_adapter.py`)
- **تجهیز `ProgressFileReader` به متد `fileno` و خصیصه `name`:** انتقال مستقیم توصیف‌گر فایل با متد `fileno()` جهت فعال‌سازی محاسبه ۱۰۰٪ خودکار و دقیق `Content-Length` توسط `aiohttp.FormData` و کلاینت HTTP.
- **جلوگیری قطعی از ارسال Chunked Transfer Encoding:** مهار ارسال هدر نامعتبر `Transfer-Encoding: chunked` به سرورها و گیت‌وی‌های Nginx بله و پذیرش بی‌درنگ استریم فایل.
- **تنظیم بافر بهینه ۶۴ کیلوبایتی:** خواندن داده‌ها در چانک‌های استاندارد ۶۴KB به منظور حفظ حداکثر پهنای باند بدون ایجاد بن‌بست پردازشی (Deadlock).

### 📊 نوار پیشرفت زنده آپلود در تلگرام (Live Upload Progress Bar) (`platforms/telegram_adapter.py`, `services/media_service.py`)
- **گزارش لحظه‌ای ۳ ثانیه‌ای در هنگام آپلود به بله:** نمایش دقیق درصد، حجم انتقال‌یافته و سرعت انتقال در تلگرام:
  ```text
  🚢 [پارت ۱ از ۲] در حال بارگذاری فایل در بله...
  [████████░░░░] \u200e65% (۲۸.۱ از ۴۳.۴ مگابایت)
  ⚡️ سرعت آپلود: 1.4 MB/s | لطفاً شکیبا باشید
  ```
- **اتصال متقابل به ارسال تکی و دسته‌ای:** ارسال بلادرنگ کال‌بک پیشرفت به متدهای `send_video`، `send_document` و `send_audio` در تمام فلوهای کاربری.

### 🚨 هشدارهای تشخیصی مستقیم در تلگرام (Direct Failure Alerts) (`platforms/telegram_adapter.py`)
- **برچیدن فریزهای خاموش در وضعیت «در حال ارسال...»:** در صورت بروز هرگونه قطعی شبکه یا اختلال سرور مقصد، وضعیت پیام فوراً با جزئیات خطا و راهنمای کلیک مجدد به‌روزرسانی می‌شود:
  `❌ خطا در ارسال به بله: [علت خطا] | لطفاً دکمه ارسال را مجدداً بزنید.`

---

## [v0.5.9] - 1403/07/12

### 🎯 جراحی فنی محاسبات درصد و شمارش معکوس فشرده‌سازی (`services/compressor.py`, `media/compressor.py`)
- **پروب دقیق مدت‌زمان فایل با FFprobe (`get_video_duration_async`):** استخراج مدت‌زمان واقعی پارت‌های برش‌خورده به جای مدت‌زمان ویدیوی اصلی ۵۳ دقیقه‌ای با دستور `ffprobe -show_entries format=duration` جهت رفع ریشه‌ای باگ قفل شدن درصد روی ۹۹٪ و صفر ماندن زمان باقیمانده (`00:00`).
- **شمارش معکوس و درصد پویا و روان:** محاسبه دقیق و بدون پرش ثانیه‌ها با کسر زمان سپری‌شده از مدت‌زمان واقعی پارت و قالب‌بندی با ایزولاسیون LTR/RTL:
  `[████████▒▒▒▒] \u200e64% (سرعت: 1.8x | باقیمانده: 01:15)`
- **روش اختصاصی برش تک‌پارت (`split_single_part_async`):** جداسازی مستقل هر پارت از طریق استریم کپی با فالبک به انکود سبک بدون نیاز به منتظر ماندن برای تمام پارت‌ها.

### ⚡️ معماری ارسال بلادرنگ گام‌به‌گام (Pipelined Execution) (`platforms/telegram_adapter.py`)
- **حذف تاخیر ۸ تا ۱۰ دقیقه‌ای آپلود:** بازطراحی کامل متد `split_and_transfer_video_to_bale` به شکل پایپ‌لاین ترتیبی؛ به محض برش و آماده‌سازی پارت ۱، ارسال فوری به بله کلید می‌خورد و پس از تایید موفقیت، پارت ۲ برش خورده و ارسال می‌شود.
- **اطلاع‌رسانی بلادرنگ مرحله‌ای:** نمایش پیام‌های تعاملی و هشدارهای تشخیصی فعال در صورت بروز خطای شبکه بدون فریز شدن ربات.

### 🚢 آپلود مقاوم به بله با چانک ۱MB و ۲ بار تلاش مجدد (`platforms/bale_adapter.py`)
- **مهار لاگ‌های خالی با `repr(e)`:** رفع نمایش پیام‌های خالی در استثناهایی نظیر `asyncio.TimeoutError` از طریق نگاشت جامع `f"{type(e).__name__}: {e or repr(e)}"`.
- **بهینه‌سازی تایم‌اوت و چانکینگ:** اعمال تایم‌اوت `aiohttp.ClientTimeout(total=300, connect=30, sock_read=90)` و تنظیم اندازه چانک‌های خوانش فایل روی ۱ مگابایت در `ProgressFileReader`.
- **فالبک دوگانه به sendDocument:** تلاش خودکار تا ۲ بار متوالی برای ارسال فایل به صورت سند در صورت دریافت خطای ۴۱۳ یا قطع ارتباط استریم در `sendVideo`.

### 📊 اصلاح نوار پیشرفت LTR/RTL در ارسال‌های دسته‌ای (`services/media_service.py`)
- **نمایش متقارن و یکدست:** استفاده از کاراکتر ایزولاسیون `\u200e` قبل از کاراکتر درصد و یونیکد استاندارد `░` در `_make_bar`.

---

## [v0.5.8] - 1403/07/11

### ⚙️ بهینه‌سازی قطعی و غیرمسدودکننده موتور ویدیویی با FFmpeg آسنکرون (True Async) (`services/compressor.py`, `media/compressor.py`)
- **رفع انسداد حلقه رویدادهای Asyncio:** اجرای کامل دستورات FFmpeg با `asyncio.create_subprocess_exec` به جای توابع همگام مسدودکننده (`subprocess.run` یا Popen بدون await) جهت رهاسازی ۱۰۰٪ ترد اصلی پایتون در هنگام پردازش ویدیوهای سنگین.
- **پارس آسنکرون خروجی پایپ و گزارش زنده ۳ ثانیه‌ای:** استخراج برخط اطلاعات پیشرفت (`out_time_us` و `speed`) و محاسبه درصد، زمان باقیمانده، سرعت پردازش و مقایسه حجم اولیه و برآورد نهایی به فرمت درخواستی:
  ```text
  ⚙️ [پارت ۱ از ۲] در حال فشرده‌سازی هوشمند ویدیو...
  [████████▒▒▒▒] 64% (سرعت: 1.8x | زمان باقیمانده: 00:45)
  📊 حجم اولیه: 76.2 MB ➔ برآورد نهایی: ~43 MB
  ```

### 🚢 پایپ‌لاین ترتیبی و مقاوم آپلود پارت‌های ویدیو به بله با تایم‌اوت ۴۵۰ ثانیه‌ای و فالبک خودکار (`platforms/bale_adapter.py`, `platforms/telegram_adapter.py`)
- **تقسیم و فشرده‌سازی خودکار ثانویه:** تقسیم آسنکرون ویدیو (`split_video_async`) و بررسی ثانویه پارت‌ها؛ در صورت عبور هر پارت از سقف ۴۸.۵ مگابایت، فشرده‌سازی خودکار تا سقف ۴۵MB اعمال می‌شود.
- **تایم‌اوت‌های مقاوم شبکه در کلاینت بله:** اعمال `aiohttp.ClientTimeout(total=450, connect=30, sock_read=120)` جهت پیشگیری قطعی از فریز بی‌پایان در هنگام آپلود فایل‌های سنگین.
- **فالبک خودکار از `sendVideo` به `sendDocument`:** در صورت دریافت خطای ۴۱۳ (حجم زیاد برای استریم) یا خطای شبکه، فایل بلافاصله به عنوان داکیومنت بدون شکست ارسال می‌شود.
- **اطلاع‌رسانی بلادرنگ مرحله‌ای:** نمایش وضعیت گام‌به‌گام ارسال و تایید موفقیت هر پارت پیش از شروع پارت بعدی:
  `🚢 در حال ارسال پارت ۱ از ۲ به بله (۴۳.۳ مگابایت)... لطفاً شکیبا باشید`
  `✅ پارت ۱ از ۲ با موفقیت به بله ارسال شد! در حال آماده‌سازی و ارسال پارت ۲...`

### 🚨 هشدارهای فعال خطای شبکه (Active Failure Alerts) (`platforms/telegram_adapter.py`)
- **حذف قطعی فریزهای خاموش:** احاطه کل فرآیند تقسیم، بهینه‌سازی و آپلود در بلوک سراسری `try...except` و اطلاع‌رسانی بلادرنگ پیام با نشانگر خطا:
  `❌ خطا در ارسال پارت به بله: [علت خطا / تایم‌اوت شبکه] | فرآیند متوقف شد.`

### 📦 صف ترتیبی FIFO برای ارسال‌های چندفایله (Sequential Batch Queue) (`services/media_service.py`, `platforms/telegram_adapter.py`)
- **مهار هجوم همزمان و اشباع پردازنده:** پیاده‌سازی کلاس `SequentialBatchQueue` بر بستر `asyncio.Queue` در `MediaService` جهت اجرای ترتیبی و گام‌به‌گام بسته‌های چندفایله (دانلود ➔ فشرده‌سازی/تقسیم ➔ ارسال به بله/روبیکا ➔ تایید پایان).
- **نمایش پویای پیشرفت فایل‌ها:** وضعیت هر فایل به شکل داینامیک و شفاف رندر می‌شود:
  `📦 فایل ۲ از ۳: [████▒▒▒] 45%`

---

### 🌐 جایگذاری ۱۰۰٪ دقیق ۱۷ دسته‌بندی استخراج‌شده زنده از سایت عباس‌منش (`services/feed_scraper.py`, `services/abasmanesh_crawler.py`)
- **خزش و استخراج اسلاگ‌ها و URLهای واقعی:** جایگزینی کامل اسلاگ‌های حدسی قبلی با آدرس‌های ۱۰۰٪ واقعی و زنده سایت عباس‌منش (نظیر `indisputable-law-of-the-universe`، `recognition-of-essential-from-nonessential`، `inner-piece`، `living-in-paradise`، `cross-country-road-trip` و دسته‌های انکودشده).
- **اصلاح الگوی صفحه‌بندی به `?page={page_number}`:** حذف ساختار اشتباه `/page/2/` و پیاده‌سازی متد استاندارد `build_page_url` با پارامتر کوئری `?page=` جهت دسترسی دقیق به تمامی صفحات آرشیو دانلودها و دسته‌بندی‌ها.
- **ماژول مستقل و جامع `services/abasmanesh_crawler.py`:** پیاده‌سازی کلاس `AbasmaneshCrawler` شامل خزش دسته‌ها، مقالات، فایل‌های صوتی و تصویری و اطلاعات درس‌نامه با مستندسازی کامل فارسی.

### 🖼️ مهار کامل تصاویر لود تنبل با متد جامع `extract_thumbnail_url` (`services/abasmanesh_crawler.py`, `services/feed_scraper.py`)
- **استخراج هوشمند کاور باکیفیت بدون پلیس‌هولدر خالی:** تحلیل و پردازش صفات وردپرس نظیر `data-src`، `data-lazy-src`، `data-original`، `srcset` و `background-image` و پالایش داده‌های خالی `data:` و تصاویر ۱x۱ پیکسل.

### ⚙️ نوار پیشرفت زنده و غیرمسدودکننده ۳ ثانیه‌ای FFmpeg (`media/compressor.py`)
- **اجرای غیرمسدودکننده فشرده‌سازی با `-progress pipe:1`:** بازنویسی اجرای FFmpeg در کلاس `SmartVideoCompressor` با استفاده از `subprocess.Popen` و پردازش بلادرنگ خطوط خروجی زمان (`out_time_us`) و سرعت (`speed`).
- **تراتل زمانی ۳ ثانیه‌ای گزارش پیشرفت:** ارسال واکنشی نوار پیشرفت شکیل به تلگرام و بله هر ۳ ثانیه یک‌بار جهت جلوگیری از محدودیت‌های شبکه و تراتل پیام‌رسان‌ها.

### 🎯 همگام‌سازی ایموجی‌های اختصاصی دسته‌ها در تلگرام، بله و وب‌پنل (`platforms/telegram_adapter.py`, `platforms/bale_adapter.py`, `services/web_panel.py`)
- **حذف آیکون پوشه ثابت و نمایش ایموجی منحصر‌به‌فرد هر دسته:** اتصال مستقیم اینلاین‌کیبوردهای تلگرام و بله به ایموجی واقعی هر دسته (`{c.get('emoji')} {c['title']}`).
- **پشتیبانی کیبورد اصلی از ساختار ۵ دکمه‌ای استاندارد `MAIN_KEYBOARD_LAYOUT`:** کاهش دکمه‌های پیش‌فرض به ۵ دکمه ارگونومیک و پشتیبانی همزمان از ساختار رشته‌ای و دیکشنری.

### 🎓 مدیریت پیشرفته دوره‌ها و ابزار ۱-کلیک ساخت دوره از دسته‌بندی (`services/store_service.py`, `app.py`, `services/web_panel.py`)
- **ابزار ۱-کلیک «✨ ساخت دوره از این دسته‌بندی»:** تولید خودکار دوره آموزشی رایگان شامل تمامی جلسات و لینک‌های مستقیم فایل‌های یک دسته‌بندی و انتقال نرم به تب دوره‌ها.
- **حذف جلسات و دوره‌ها با تاییدیه و بدون رفرش:** پیاده‌سازی متد `delete_course_episode` در بک‌اند و فرانت‌اند جهت مدیریت سرفصل‌ها و اکشن حذف بدون رفرش کارت دوره.

### 🎨 بهینه‌سازی تایپوگرافی و برچیدن کامل واژه VIP (`services/web_panel.py`)
- **قفل سراسری فونت وزیرمتن (Vazirmatn):** فونت یکپارچه و بهینه‌سازی استایل‌ها متناسب با متغیرهای تم CSS.
- **جایگزینی کامل واژه VIP:** تغییر کلیه متون، هدرها و برچسب‌های باقی‌مانده به «اشتراک پریمیوم».

---

## [v0.5.6] - 1403/07/09

### 🚀 رفع ریشه‌ای خطای سریالایز `aiohttp` با بازنویسی کلاس `ProgressFileReader` (`platforms/bale_adapter.py`)
- **پشتیبانی کامل از استاندارد `io.IOBase` و پیلُود `aiohttp`:** ارتقاء کلاس `ProgressFileReader` به عنوان زیرکلاس رسمی `io.IOBase` به همراه متدهای حیاتی `read`, `readinto`, `seek`, `tell`, `seekable`, `readable`, `writable`, `close` و `__len__`.
- **ثبت رسمی به عنوان پیلُود `aiohttp`:** اتصال `payload.register_payload(ProgressFileReader, payload.IOBasePayload)` جهت مهار دائمی خطای `TypeError: Can not serialize ProgressFileReader` در متد `aiohttp.FormData.add_field`.

### 🛡️ مهار نشت خطاهای شبکه و SSL هوش مصنوعی و نرمال‌سازی فازی منوها (`services/ai_service.py`, `core/formatters.py`, `platforms/bale_adapter.py`, `platforms/telegram_adapter.py`)
- **پیشگیری از پاسخ خطا به کاربر در خرابی هوش مصنوعی:** جایگزینی خطاهای اتصال شبکه و DNS (مانند `Can not connect to host api.vyceai.com`) با پیام‌های آرام‌بخش و راهنمای منوی ربات به جای پرینت استک‌ترِیس خام.
- **اکشن‌های کانونیکال و نرمال‌سازی فازی:** حذف وابستگی هندلرهای پیام به تطابق رشته‌ای خام، تعریف اکشن‌های کانونیکال استاندارد (`ACTION_TODAY_SIGN`, `ACTION_PREMIUM`, `ACTION_FREE_DOWNLOADS`, `ACTION_PRODUCTS`, `ACTION_FREQUENCY`, `ACTION_USER_ACCOUNT`, `ACTION_SUPPORT`) و مهار دائمی ارسال کلیک روی گزینه‌هایی مانند «دانلودها (هدیه) 📁» به هوش مصنوعی.

### ✂️ سقف سختگیرانه ۴۸.۵MB برای ارسال مستقیم بدون اسپلیت به بله (`platforms/telegram_adapter.py`)
- **ارسال مستقیم فایل‌های تا ۴۸.۵ مگابایت:** کلیه ویدیوها و فایل‌های کمتر یا مساوی ۴۸.۵MB به صورت مستقیم و بدون نمایش منوی تقسیم به بله ارسال می‌شوند.
- **دستیار هوشمند دوگزینه‌ای برای فایل‌های بالای ۴۸.۵MB:** برای فایل‌های بزرگتر منحصراً دو گزینه استاندارد `✂️ تقسیم هوشمند به ۲ پارت` و `🗜️ فشرده‌سازی هوشمند تا سقف بله` نمایش داده می‌شود.

### ⚡️ کش دیسکی فوق‌سریع جلسات ۱۶ دسته‌بندی عباس‌منش (`services/feed_scraper.py`)
- **پاسخ‌دهی زیر ۵۰ میلی‌ثانیه به درخواست‌های جلسات دسته‌ها:** پیاده‌سازی متدهای `load_category_disk_cache` و `save_category_disk_cache` در فایل پایدار `data/abasmanesh_cache.json` جهت دسترسی بلادرنگ و مقاومت کامل در برابر نوسانات اینترنت.

### 📅 تقارن کامل تاریخ‌های سه‌گانه نشانه امروز (`core/jalali.py`, `core/sign_service.py`)
- **همگام‌سازی کامل تاریخ شمسی، قمری و میلادی:** پیاده‌سازی متدهای `gregorian_to_hijri` و `get_synchronized_date_string` بر اساس قانون دوقلوهای همسان در بله و تلگرام (`📅 شنبه ۲ آبان ۱۴۰۵ | ۲۳ ربیع‌الثانی ۱۴۴۸ | 24 October 2026`).

### 🎛️ شخصی‌ساز ویژوال کیبورد بات‌ها در وب‌پنل (Visual Keyboard Customizer) (`services/web_panel.py`, `app.py`, `core/database.py`, `platforms/bale_adapter.py`, `platforms/telegram_adapter.py`)
- **آکاردئون پیشرفته چیدمان کیبورد در تب تنظیمات:** رابط کاربری Material 3 با متغیرهای تم CSS، پیش‌نمایش تعاملی قاب گوشی تلفن همراه و شبیه‌سازی زنده کیبورد تلگرام و بله.
- **انعطاف در جابجایی دکمه‌ها و سطرها:** افزودن، حذف و تغییر موقعیت افقی و عمودی دکمه‌ها و سطرها به همراه دکمه‌های بازنشانی به پیش‌فرض و ذخیره آنی در `data/settings.json` و دیتابیس بدون رفرش صفحه (Zero Page-Reload).
- **بهینه‌سازی رابط کاربری لاگ‌ها:** نمایش کنترل‌های کنسول لاگ منحصراً در حالت `:hover` جهت خلوت و مدرن ماندن پنل.
- **یکپارچه‌سازی نام تب دانلودها:** بازتاب سراسری عنوان به «دانلودها (ویژه مشترکین پریمیوم)».

---

## [v0.5.5] - 1403/07/08

### ⚡️ لود فوق‌سریع وب‌پنل زیر ۵۰۰ms و بارگذاری تنبل (`services/web_panel.py`, `services/feed_scraper.py`)
- **حذف کامل درخواست‌های مسدودکننده فید از لود اولیه:** برچیدن فراخوانی‌های همزمان `fetchFeedDownloads` و `loadFeedCategories` از رویداد سراسری `DOMContentLoaded` و انتقال به لود تنبل (Lazy Load) تنها هنگام ورود کاربر به تب دانلودها.
- **کش دیسکی مقالات و دانلودها (`data/feed_cache.json`):** پیاده‌سازی متدهای `load_feed_disk_cache` و `save_feed_disk_cache` جهت پاسخگویی به درخواست‌های فید در کمتر از ۱۰ میلی‌ثانیه بدون انتظار برای ریکوئست‌های شبکه.
- **روت و دکمه به‌روزرسانی غیرمسدودکننده کش (`/api/feed/refresh`):** اضافه شدن دکمه «به‌روزرسانی کش دیسک» در هدر تب دانلودها که فرآیند اسکرپ و تازه‌سازی داده‌ها را در پس‌زمینه (Background Thread) اجرا کرده و فوراً پاسخ ۲۰۰ را بدون فریز پنل بازمی‌گرداند.

### 🔮 حل ریشه‌ای خطای ۴۰۰ تلگرام در نشانه امروز و اولویت cdnir (`core/sign_service.py`, `services/feed_scraper.py`, `platforms/telegram_adapter.py`, `platforms/bale_adapter.py`)
- **ریشه‌کنی خطای `[400 WEBPAGE_CURL_FAILED]`:** حذف متد ناامن ارسال URL مستقیم به `reply_audio` در تلگرام؛ در صورت عدم دانلود موفق صوت در سرور، پیام متنی جامع به همراه دکمه‌های شیشه‌ای لینک مستقیم ارسال می‌شود و سرور تلگرام هیچ ریکوئست curl شکست‌خورده‌ای اجرا نمی‌کند.
- **ترجیح قطعی CDN پرسرعت ایران (`cdnir.abasmanesh.com`):** جایگزینی کلیه دامنه‌ها و لینک‌های منسوخ و ۴۰۴ موجود در `FALLBACK_ITEMS` و تبدیل هوشمند آدرس‌های قدیمی `cdneu` به `cdnir` در زمان استخراج رسانه‌ها.
- **فالبک دانلود دوگانه در `ensure_audio_downloaded`:** آزمایش همزمان کاندیداهای دامنه‌های مختلف تا از دانلود ۱۰۰٪ قطعی فایل صوتی باینری در سرور اطمینان حاصل شود.

### 📄 پاکسازی کامل متون انتقال فایل در بله و تلگرام (`platforms/bale_adapter.py`, `platforms/telegram_adapter.py`)
- **حذف قطعی متون پیش‌فرض سیستم:** حذف کامل عبارات زائد «✅ منتقل شده از تلگرام» و «✅ منتقل شده از بله» از تمام پیام‌ها و کپشن‌های انتقال تکی و چندتایی فایل و حفظ نام پاک فایل (`📄 filename.mp4` یا `📄 پارت ۱ از ۳: ...`).

### 📅 نمایش تاریخ و ساعت شمسی در اشتراک پریمیوم و تمدید سریع (`core/jalali.py`, `services/user_service.py`, `services/web_panel.py`, `app.py`)
- **محاسبه دقیق تاریخ، ساعت و روز هفته شمسی:** پیاده‌سازی متد `format_jalali_full` با ارقام فارسی و روزهای هفته (مانند: `شنبه ۲ آبان ۱۴۰۵ ساعت ۰۸:۵۳`).
- **دکمه‌های سریع `[ +۱۰ روز ]` و `[ +۳۰ روز ]` در وب‌پنل:** اضافه شدن دکمه‌های مجزای تمدید ۱۰ و ۳۰ روزه به همراه دکمه لغو در جدول کاربران و پاسخگویی آنی ایجکس با تاریخ شمسی انقضا.
- **نوتیفیکیشن تبریک در بله و تلگرام:** ارسال پیام آنی اطلاع‌رسانی تمدید یا اعطای اشتراک به کاربران در پیام‌رسان با ذکر تاریخ شمسی انقضا.

### 🌴 ایموجی‌های اختصاصی ۱۶ دسته‌بندی و ادیتور زنده وب‌پنل (`services/feed_scraper.py`, `services/web_panel.py`, `app.py`)
- **تخصیص ۱۶ ایموجی استاندارد و پرمعنا:** سریال تمرکز بر نکات مثبت (🌴)، زندگی در بهشت (🗽)، سفر به آمریکا (🛣️)، مصاحبه و لایوها (🎙)، قوانین بدون تغییر (⚖️)، توحید در عمل (🕋)، اصل از فرع (🎯)، ایمانی که عمل می‌آورد (⚡️)، کنترل ذهن (🧠)، باورهای ثروت‌ساز (💰)، برنامه‌نویس زندگی (💻)، صلح با خود (🕊)، سرمایه‌گذاری روی خود (💎)، آرامش در پرتو آگاهی (🕯)، قدم‌های تکاملی (🪜) و کلیدها (✨).
- **ویرایشگر آنلاین عناوین و ایموجی‌ها در وب‌پنل:** فرم اینلاین ویرایش هر ۱۶ دسته در ساب‌تب پریمیوم و دکمه «ذخیره تغییرات دسته‌ها» متصل به روت `/api/categories/update` با ذخیره دائمی در `data/custom_categories.json`.

### 📊 نوار پیشرفت زنده بایت‌به‌بایت آپلود بله در انتقال گروهی (`platforms/bale_adapter.py`, `platforms/telegram_adapter.py`)
- **کلاس استریم `ProgressFileReader`:** خواندن چانک‌های باینری و محاسبه دقیق پیشرفت آپلود فایل به سرور بله.
- **به‌روزرسانی پیام تلگرام با نام پارت و درصد:** نوار پیشرفت استاندارد با ذکر `📄 فایل ۲ از ۳: filename`، سرعت انتقال، مگابایت و درصد زنده.

---

## [v0.5.4] - 1403/07/07

### 🖥 رفع ریشه‌ای سیاهی تب‌های وب‌پنل (`services/web_panel.py`)
- **رفع خطای بسته نشدن تگ div در تب دانلودها:** با بستن تگ div هدر بالای نوار دسته‌بندی‌ها، تب‌های «کاربران»، «سکرت‌ها»، «تنظیمات» و «فرکانس» از حالت فرزندی تگ پنهان `tab-downloads` خارج شده و به صورت کاملاً مستقل و بی‌نقص در کانتینر اصلی صفحه رندر شدند.
- **تاب‌آوری جدول کاربران:** افزودن بلوک `try...catch` به ازای تک‌تک سطرهای کاربران در متد `renderUsersTable` جهت پیشگیری قطعی از کرش جدول در صورت وجود فیلدهای خالی یا نامعتبر.
- **فعال‌سازی متد `openEditCourseModal`:** اتصال مستقیم و امن دکمه ویرایش دوره‌ها به متد بهبودیافته `openEditModalById` با جستجوی هوشمند در کش دوره‌ها.
- **اصلاح یوآی:** حذف آیکون‌های شش‌نقطه‌ای زائد (⋮⋮) از کنار ساب‌تب‌های محصولات و پیاده‌سازی بج داینامیک انیمیشنی هاور برای تب دانلودها («۹۵۸ فایل» در حالت عادی و «۳۹ صفحه» در هاور).

### ⚡️ ارسال مستقیم و آنی ویدیوهای زیر سقف ۴۵MB به بله (`platforms/telegram_adapter.py`, `media/compressor.py`)
- **حذف قطعی شرط مدت‌زمان برای فشرده‌سازی:** تفکیک و مهار شرط ورود به منوی اسپلیت؛ هر فایلی با حجم کمتر یا مساوی ۴۵ مگابایت (حتی با مدت زمان‌های طولانی بالای ۱۰ دقیقه) مستقیماً و بدون نمایش منوی اسپلیت، سوال یا کاهش کیفیت به بله ارسال می‌گردد.
- **نمایش منوی تقسیم صرفاً برای فایل‌های بالای ۴۵MB:** منوی تصمیم‌گیری تقسیم و فشرده‌سازی منحصراً برای فایل‌هایی که حجم واقعی آن‌ها بالای ۴۵ مگابایت باشد نمایش داده می‌شود.

### 📊 نوار پیشرفت انتقال به بله استاندارد تلگرامی (`platforms/telegram_adapter.py`)
- بازطراحی نوار پیشرفت با کادر ۲۰ کاراکتری، درصد، حجم جابجاشده و سرعت بر ثانیه:
  ```text
  📤 در حال انتقال فایل به بله...
  [██████████░░░░░░░░░░] 50%
  📦 حجم: 16.00 MB از 32.59 MB
  ⚡️ سرعت انتقال: 4.12 MB/s
  ```

### 🛍 تفکیک کامل محصولات آموزشی از اشتراک پریمیوم (`platforms/bale_adapter.py`, `platforms/telegram_adapter.py`)
- **کیبورد ۳ ردیفه استاندارد مشتری در بله و تلگرام:**
  - ردیف اول (بزرگ و تکی): `[ 🛍 محصولات آموزشی ]` (شامل دوره‌های آموزشی و کتاب‌های صوتی)
  - ردیف دوم (متوازن): `[ 🔮 نشانه امروز من ]` | `[ 💎 اشتراک پریمیوم ]`
  - ردیف سوم: `[ 📂 دانلودها (هدیه) ]` | `[ 👤 حساب کاربری ]`
- تفکیک کامل منوی اشتراک پریمیوم (منحصراً شامل ۱۶ دسته‌بندی مقالات، فرکانس فراوانی و عضویت ماهانه) از محصولات فروشگاهی.

### 🔮 حل قطعی و تثبیت «نشانه امروز من» (`core/sign_service.py`, `services/feed_scraper.py`)
- پاکسازی کش‌های قدیمی فاقد متن و غنی‌سازی خودکار نشانه‌ها با گزیده متن درس و آموزش.
- تجهیز پیام نشانه به دکمه‌های مستقیم دانلود صوت، ویدیو، مشاهده در سایت و عضویت پریمیوم بدون تاخیر.
- پشتیبانی از استخراج تصاویر از استایل‌های `background-image` در اسکرپر سایت.

---

## [v0.5.3] - 1403/07/06

### ✂️ اصلاح متد فشرده‌سازی پارت‌های اسپلیت و دریافت عدد دستی (`media/compressor.py`, `platforms/telegram_adapter.py`)
- **رفع خطای `type object 'SmartVideoCompressor' has no attribute 'compress_video'`:**
  - اصلاح فراخوانی در تقسیم ویدیو به متد استاندارد `SmartVideoCompressor.compress_if_needed(part_path, target_max_mb=safe_limit_mb)`.
  - ایجاد نام مستعار محافظتی `compress_video = compress_if_needed` در سطح کلاس جهت جلوگیری ابدی از خطاهای نام.
  - خلوت‌سازی منوی دستیار تقسیم هوشمند ویدیو در تلگرام به دقیقاً ۳ کلید: `[ ✂️ تقسیم هوشمند به ۲ پارت ]`، `[ 🗜 فشرده‌سازی تا سقف بله ]` و `[ 🔙 بازگشت به منوی رسانه ]`.
  - پشتیبانی کامل از تایپ عدد دستی توسط کاربر در چت تلگرام (مانند ۳، ۴ یا اعداد فارسی) جهت تقسیم ویدیو به تعداد پارت‌های دلخواه کاربر با فشرده‌سازی خودکار در صورت عبور از سقف ۴۵MB بله.

### 🌐 استخراج ۱۰۰٪ پویا از مگامنوی سایت رسمی عباس‌منش (`services/feed_scraper.py`, `app.py`)
- **حذف کامل اسلاگ‌ها و عناوین هاردکد انگلیسی:**
  - استخراج داینامیک ۱۶ دسته‌بندی منوی «دانلودها (هدیه)» از ساختار DOM صفحه اصلی `https://abasmanesh.com/fa/` مستقیماً از تگ‌های `a.public-nav__dropdown-link` و کش ۲۴ ساعته در حافظه سرور.
  - افزودن روت وب‌سرویس `/api/feed/categories` جهت بازتاب بی‌درنگ دسته‌بندی‌های زنده در وب‌پنل.
  - نوار فیلتر و انتخاب دسته‌بندی در تب دانلودها در وب‌پنل جهت جابجایی نرم بین دسته‌ها.

### 🔮 استخراج متن کامل درس برای «نشانه امروز من» (`services/feed_scraper.py`, `core/sign_service.py`)
- **غنی‌سازی پیام نشانه با گزیده محتوا:**
  - استخراج پاراگراف‌های اصلی متن درس و آموزش از صفحه مقاله در سایت و درج آن در متن پیام نشانه تحت عنوان `گزیده پیام و آموزش درس`.

### 🛡 رفع خطای `name 'escape' is not defined` در بله (`platforms/bale_adapter.py`)
- ایمپورت صریح `import html` و `from html import escape` در ابتدای فایل جهت جلوگیری از کرش هنگام ساخت پیام‌ها و فاکتورها.

### 🎨 اصلاحات فرانت‌اند وب‌پنل (`services/web_panel.py`)
- اولویت‌دهی به صفت `data-src` در استخراج تصاویر کارت‌های دانلودی جهت مهار پلیس‌هولدرهای خالی SVG سایت مبدا.
- تغییر نام پیش‌فرض تب سایدبار به «دانلودها (هدیه)».

---

## [v0.5.2] - 1403/07/05

### 🎨 بازطراحی کارت‌های هدیه و یوآی وب‌پنل (Gift Cards Redesign & Web Panel)
- **کارت‌های دانلودی هدیه به سبک سایت عباس‌منش (`services/web_panel.py` و `services/feed_scraper.py`):**
  - استخراج قدرتمند تصویر شاخص مقالات با پشتیبانی از `data-src`, `data-lazy-src`, `data-original` و رزولوشن‌های بالای `srcset`.
  - مجهز شدن تصاویر کارت‌ها به `referrerpolicy="no-referrer"` و `loading="lazy"` جهت لود تضمینی و پرسرعت.
  - بازطراحی به ساختار کارت‌های ۱۶:۹ با ارتفاع ثابت، دکمه‌های شکیل وکتور SVG بدون ایموجی متنی، بج‌های وضعیت و دکمه انتقال به ربات.
  - تغییر نام تب به «فایل‌های دانلودی هدیه» و ذخیره‌سازی ماندگار تغییر نام تب‌ها در `localStorage` با قابلیت ویرایش دو کلیکه (`inlineRenameTab`).

### ✂️ حق انتخاب دستی پارت‌های اسپلیت (`media/compressor.py` و `platforms/telegram_adapter.py`)
- **اعطای اختیار کامل به کاربر برای تقسیم ویدیو:**
  - ارائه گزینه‌های شفاف در تلگرام: `[ ✂️ تقسیم به ۲ پارت ]` | `[ ✂️ تقسیم به ۳ پارت ]` | `[ 🗜 فشرده‌سازی معمولی ]`.
  - احترام کامل به پارت انتخابی کاربر و فشرده‌سازی خودکار هوشمند هر پارت در صورت عبور از ۴۸.۵ مگابایت تا سقف ۴۵MB جهت مهار خطای ۴۱۳ بله.

### 📱 بهینه‌سازی کیبورد و ناوبری بله (`platforms/bale_adapter.py`)
- **اصلاح چیدمان کیبورد اصلی مشتری:**
  - ردیف اول: `[ 🔮 نشانه امروز من ]` در راست و `[ 💎 محصولات و اشتراک پریمیوم ]` در چپ بر پایه ساختار استاندارد RTL بله.
  - حذف «فرکانس فراوانی» از منوی ریشه و انتقال انحصاری به منوی شیشه‌ای «💎 اشتراک پریمیوم».
  - پشتیبانی از تمام ورودی‌های متنی جدید و اتصال مستقیم به هاب محصولات و اشتراک.
  - اصلاح دکمه ناوبری ۱۶ دسته‌بندی به `[ 🔙 بازگشت به دسته‌بندی‌ها ]`.

### 🔮 فالبک هوشمند و پایدار نشانه امروز (`core/sign_service.py` و `platforms/`)
- **تضمین تحویل محتوا بدون سوختن پیام:**
  - در صورت تاخیر یا شکست دانلود/ارسال صوت در بله یا تلگرام، متن الهام‌بخش نشانه بلافاصله همراه با دکمه شیشه‌ای «📥 دانلود مستقیم از سایت» تحویل کاربر می‌گردد.

### 🎉 نوتیفیکیشن آنی فعال‌سازی پریمیوم (`app.py` و `platforms/bale_adapter.py`)
- **اطلاع‌رسانی فوری در پلتفرم‌ها:**
  - ارسال پیام تبریک پس از فعال‌سازی پریمیوم توسط ادمین در وب‌پنل یا پرداخت آنلاین در بله/تلگرام:
    `🎉 تبریک! اشتراک پریمیوم ۳۰ روزه شما با موفقیت فعال شد. هم‌اکنون به ۱۶ دسته‌بندی و فرکانس فراوانی دسترسی دارید. ✨`

---

## [v0.5.1] - 1403/07/04

### 🚀 رفع قطعی خطای Union و پایداری وب‌پنل (Web Panel & Runtime Stabilization)
- **تزریق سراسری تایپینگ به Builtins (`core/config.py`, `services/web_panel.py`, `app.py`):**
  - تزریق خودکار و ایزوله نمادهای تایپینگ (`Union`, `Optional`, `List`, `Dict`, `Tuple`, `Any`, `Callable`) در شیء سراسری `builtins`.
  - ریشه‌کنی قطعی و صددرصدی خطای `Error rendering dashboard: name 'Union' is not defined` در هر نقطه از چرخه اجرای پایتون و مهار خطای ۵۰۰.
  - تست و اعتبارسنجی زنده رندر بیش از ۶۶۸ کیلوبایت HTML داشبورد با کد وضعیت ۲۰۰ OK.

### ✂️ اصلاح داینامیک پارت‌های اسپلیت ویدیو برای سقف ۵۰MB بله (`media/compressor.py` و `platforms/telegram_adapter.py`)
- **حاشیه امن ۴۵ مگابایت جهت مهار ارور ۴۱۳ بله (Bale Safe Margin 45MB):**
  - بازنویسی الگوریتم محاسبه پارت‌های اسپلیت با فرمول `min_required_parts = max(2, math.ceil(total_mb / 45.0))` و ثابت `SAFE_BALE_PART_LIMIT_MB = 45.0`.
  - مهار تقسیم نامناسب ویدیوی ۱۵۳.۳ مگابایتی به ۲ پارت ۷۶.۶ مگابایتی و هدایت قطعی به ۴ پارت ۳۸ مگابایتی باکیفیت و بدون افت وضوح.
  - اعمال سقف حداکثری ۴۵ مگابایت حتی در صورت درخواست صریح کاربر برای تعداد پارت‌های کمتر.
  - اضافه شدن مکانیزم فشرده‌سازی خودکار اضطراری در صورتی که هر پارت تولیدشده از ۴۸.۵ مگابایت عبور کند.
- **به‌روزرسانی دکمه‌ها و دستیار رسانه در تلگرام:**
  - هماهنگی دکمه‌های اینلاین هشدار کیفیت و منوی اسپلیت جهت پیشنهاد هوشمند پارت‌ها بر اساس سقف ۴۵MB: `✂️ تقسیم هوشمند به {rec_parts} پارت باکیفیت`.
  - اصلاح هندلر ارسال پارت‌ها به بله با درج دقیق شماره پارت در کپشن (`پارت X از Y`) و مکث هوشمند ۱ ثانیه‌ای میان ارسال پارت‌ها جهت پیشگیری از تراتل بله.

---

## [v0.5.0] - 1403/07/04

### 🚀 رفع بحرانی و پایداری قطعی (Critical Fixes & Stabilization)
- **رفع خطای لود نشدن داشبورد وب‌پنل (`services/web_panel.py`):**
  - برطرف شدن قطعی ارور ۵۰۰ `Error rendering dashboard: name 'Union' is not defined` با اضافه کردن صریح `from typing import Union, Optional, List, Dict, Any, Tuple` در خطوط آغازین فایل.
  - بازیابی رندر کامل و بی‌نقص رابط کاربری داشبورد وب‌پنل با کد ۲۰۰ OK.
- **رفع خطای UnboundLocalError در SmartVideoCompressor تلگرام (`platforms/telegram_adapter.py`):**
  - حذف کلیه ایمپورت‌های سایه‌انداز محلی (`from media.compressor import SmartVideoCompressor, SmartAudioCompressor`) درون بدنه متد `media_callbacks` که اسکوپ متغیرهای ماژول را محلی می‌کرد و منجر به ارور خط ۳۶۳۹ می‌شد.
  - تضمین دسترسی ایمن به متد `SmartVideoCompressor.precalculate_video_quality` از ایمپورت سراسری سطح فایل.
  - پایداری کامل فرآیند پیش‌محاسبه کیفیت و انتقال خودکار و بدون مکث ویدیوها و فایل‌های صوتی به بله پس از اتمام دانلود.

### 📜 استانداردهای منشور مهندسی (Engineering Guidelines)
- **ثبت بند ۵.۹ در `AGENTS.md` (قانون ممنوعیت مطلق ایمپورت‌های محلی و سایه‌انداز):**
  الزام قطعی به استفاده از ایمپورت‌های سراسری Top-level جهت پیشگیری از خطاهای اسکوپینگ پایتون.
- **اعتبارسنجی اجباری پیش از کامیت (Pre-Commit Syntax Validation):**
  الزام اجرای کامپایل با `python -m py_compile` برای تمامی ماژول‌های تغییریافته قبل از هرگونه کامیت گیت.

---

## [v0.4.9] - 1403/07/04

### 🚀 رفع بحرانی و هات‌فیکس‌ها (Critical Fixes)
- **رفع خطای چرخه کرش aiohttp در بله (`platforms/bale_adapter.py`):**
  حذف کامل دستور سایه‌انداز `import aiohttp` درون تابع محلی پولینگ و بازگرداندن اسکوپ ماژول گلوبال، مهار خطای پرتکرار `cannot access local variable 'aiohttp' where it is not associated with a value` که منجر به قفل شدن حلقه استعلام و اشغال هسته سرور می‌شد.
- **تثبیت استریم دانلود و رفع ناهماهنگی حجم در تلگرام (`services/media_service.py` و `platforms/telegram_adapter.py`):**
  - استخراج مستقیم و عمیق `file_size` از آبجکت‌های `video`، `audio` و `document` پیام تلگرام.
  - اعمال تراتل سخت‌گیرانه در بازه‌های ۱۰ درصدی یا حداقل ۲ ثانیه روی هوک پیشرفت جهت محافظت از FloodWait و عدم فریز کلاینت.
  - تضمین فلاش بافر روی دیسک با `f.flush()` و `os.fsync()` قبل از هرگونه تغییر نام فایل موقت.
  - جلوگیری قطعی از نمایش ۱۰۰٪ مگر پس از اعتبارسنجی کامل بایت‌های فایل روی حافظه دیسک فیزیکی.

### 🌐 وب‌پنل و رابط کاربری (Web Panel UI/UX)
- **احیای کامل جدول هدایای دانلودی رایگان (`services/feed_scraper.py` و `services/web_panel.py`):**
  - بازنویسی کامل پارسر با پشتیبانی از گرید مدرن مقالات (`div.article-grid` و کارت‌های `div.card`).
  - تفکیک کامل کارت‌های محتوایی از لینک‌های منو و کشوی ناوبری سایت (`public-nav`, `public-drawer`, `public-bottom-bar`).
  - استخراج دقیق لینک‌های صوتی و تصویری مستقیم، کاورهای باکیفیت و دسته‌بندی موضوعی با اعتبارسنجی زنده.
- **افزودن Drag & Drop پایدار به ۳ ساب‌تب محصولات (`[دوره‌های آموزشی]`, `[کتاب‌های صوتی]`, `[اشتراک پریمیوم]`):**
  - مجهز شدن ساب‌تب‌های هاب محصولات به قابلیت جابجایی ترتیبی با درگ‌ان‌دراپ دسکتاپ و تاچ موبایل.
  - ذخیره‌سازی و بازیابی ماندگار چیدمان منتخب ادمین در `localStorage` مرورگر (`unfinit_products_subtabs_order`).
- **ارتقاء کارت اشتراک پریمیوم به ۱۶ دسته‌بندی رسمی عباس‌منش:**
  - جایگزینی ۵ پروژه استاتیک با ۱۶ دسته‌بندی رسمی مقالات و هدایای سایت عباس‌منش در تب محصولات.
  - نمایش تفکیک‌شده شناسه‌ها، عناوین، برچسب اشتراک پریمیوم و پیوندهای مستقیم به سایت با اسکرول ارگونومیک تم فعال.

### 📜 استانداردهای معماری (Engineering Rules)
- **افزودن بند ۵.۸ به منشور مهندسی `AGENTS.md` (قانون طلایی بازتاب متناظر بک‌اند در وب‌پنل):**
  الزام قطعی به ایجاد کنترل‌ها و بازتاب بصری هر ویژگی و قابلیت جدید بک‌اند به صورت همگام در وب‌پنل.

---

## [v0.4.8] - 1403/07/03

### تغییرات کلیدی
- **تفکیک دوگانه محتوای عباس‌منش:** سطح اول نشانه امروز من به عنوان خدمت رایگان و سطح دوم ۱۶ دسته‌بندی هدیه و آرشیو به عنوان خدمات انحصاری باشگاه اشتراک پریمیوم.
- **اسکرپر داینامیک:** ماژول `services/feed_scraper.py` جهت استخراج پویا و عدم هاردکد هیچ‌گونه لینک محتوایی.
- **بهینه‌سازی استریم دانلود تلگرام:** ایجاد فایل موقت `.turbo_*.part` و فلاش بافر.
- **ارگونومی ناوبری موبایل:** تنظیم اکشن‌های انقضادار و نوار پایینی دسترسی سریع.

---

## [v0.4.7] - 1403/07/02

### تغییرات کلیدی
- **دستیار هوشمند کمپرس یا اسپلیت ویدیو:** محاسبه پیش از پردازش نسبت حجم به زمان و نمایش اخطار افت شدید کیفیت همراه با پیشنهاد تقسیم هوشمند پارت‌ها.
- **دانلود توربو استریم:** پیاده‌سازی متد `turbo_download_telegram` با چانک‌های بافر ۲ مگابایتی.
- **سوییچ‌های متادیتا و تغییر نام خودکار:** سوئیچ‌های `APPLY_DEFAULT_ARTIST_TAG` و `AUTO_RENAME_FILE_TO_TITLE`.
- **ساماندهی استودیو و نوار پایین موبایل:** حذف افزونگی‌ها و استفاده از استاندارد Material 3.

---

## [v0.4.6] - 1403/07/01

### تغییرات کلیدی
- تجمیع تب محصولات در هاب ۳ شاخه‌ای (دوره‌ها، کتاب‌های صوتی، اشتراک پریمیوم).
- برچیدن کامل واژه VIP و الزام عبارت استاندارد «اشتراک پریمیوم».
- مهار کامل استثناهای شبکه `BrokenPipeError` در خروجی وب‌سرور.
- ایجاد صف امن ذخیره‌سازی محلی فایل‌ها برای سروش‌پلاس در زمان قطعی شبکه.
