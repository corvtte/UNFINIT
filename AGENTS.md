# UNFINIT Engineering Guidelines

این فایل قوانین دائمی مهندسی پروژه UNFINIT را برای تمام عامل‌های هوش مصنوعی تعریف می‌کند.
هر agent یا مدل هوش مصنوعی که روی این پروژه کار می‌کند موظف است این فایل را پیش از هر اقدامی بخواند.

---

## 1. نسخه‌گذاری و ثبت گیت

### 1.1 ارتقاء خودکار نسخه

- نسخه فعلی را در core/config.py بررسی کن.
- پس از اتمام تسک، نسخه را یک پله Minor ارتقا بده.
- نسخه را در دو مکان به‌روزرسانی کن:
  1. مقدار پیش‌فرض ENGINE_VERSION در core/config.py
  2. متن نمایشی engine_version در get_system_health() در services/web_panel.py

### 1.2 ثبت کامیت گیت

    git add -A
    git push origin main

انواع معتبر: feat, fix, refactor, chore, docs

### 1.3 همگام‌سازی هاگینگ‌فیس

    python scripts/hf_manager.py sync

هیچ تسکی بدون پوش گیت‌هاب و سینک هاگینگ‌فیس کامل نیست.

---

## 2. گزارش‌دهی (Reporting)

### 2.1 ساختاردهی راست‌به‌چپ (RTL Standard)
کلیه خروجی‌ها، گزارش‌ها، پیام‌ها و توضیحات فارسی ایجنت در ترمینال و محیط چت باید با ساختار استاندارد RTL و بدون به‌هم‌ریختگی براکت‌ها، پرانتزها و علائم نگارشی ارائه شوند. عبارات انگلیسی یا فنی داخل متن باید به صورت مجزا یا با کدبلاک/بک‌تیک مشخص شوند تا چینش پاراگراف مختل نگردد.

در پایان هر پاسخ جدول خلاصه تغییرات فارسی ارائه بده:

| ردیف | ماژول / فایل | تغییر اعمال‌شده | وضعیت |
|:---:|:---|:---|:---:|

---

## 3. امنیت (Security)

- هیچ‌گاه رمز عبور یا توکن هاردکدشده در کد قرار نده.
- احراز هویت ادمین فقط از ADMIN_PANEL_PASSWORD محیطی.
- .env، session، db همیشه در .gitignore باشند.

---

## 4. تمیزکاری کد و معماری (Clean Code & Architecture)

- فایل‌های موقت بعد از استفاده حذف کن.
- قبل از کامیت سینتکس را بررسی کن.
- **معماری کاملاً آینه‌ای (Identical Twin Architecture):** تمامی قابلیت‌های کاربری (ثبت‌نام و هویت واحد، کیبوردها، سیستم رفرال و ویروسی، سبد خرید، تعهدنامه و قوانین دسترسی) در بله و تلگرام باید از یک منطق مشترک (`services/user_service.py` و `services/referral_service.py`) تغذیه کرده و تجربه کاربری و دکمه‌ها در هر دو پلتفرم کاملاً همسان و متقارن باشد.
- **معماری ماژولار و تفکیک‌شده جاوااسکریپت (Sandboxed JavaScript):** کلیه کدهای کلاینت در وب‌پنل باید در ۴ ماژول مجزای خوداجرا (IIFE) به همراه `try...catch` ایزوله شوند (ناوبری، استودیو، فروشگاه/سفارشات، هوش مصنوعی/تنظیمات). توابع عمومی باید روی شیء `window` اکسپورت شوند و رویدادهای ناوبری از طریق Event Delegation با `data-tab` مدیریت گردند.
- **ممنوعیت مطلق هاردکد محتوای تجاری و لینک‌ها:** هیچ نام دوره‌ای، نام فایلی، یا مقادیر استرینگ فرضی (مانند `ref_bale` و `ref_telegram`) نباید در کدها هاردکد شوند؛ تمام عناوین و لینک‌ها باید به صورت ۱۰۰٪ پویا از دیتابیس و وضعیت زنده ربات استخراج شوند.
- **تضمین ماندگاری دوره‌ها در استارت‌آپ:** در فایل `core/database.py` متد `init_db` هرگز و تحت هیچ شرایطی نباید دوره‌های ثبت‌شده توسط کاربر در `courses.json` یا جدول `products` را فیلتر، ریست یا حذف نماید.
- **معماری تک‌کانتینری Drag & Drop:** از تکثیر (Clone) المان‌های DOM تب‌ها برای موبایل خودداری شود؛ یک نوار تب واحد با پشتیبانی همزمان رویدادهای Touch و Mouse پیاده‌سازی شود تا تجربه کاربری در ویندوز و اندروید کاملاً یکسان باشد.
- **ممنوعیت نام‌های ساختگی ربات:** در لینک‌های دعوت، نام کاربری ربات بله باید مستقیماً از خروجی زنده `get_me()` خوانده شود و نام فرضی `UNFINIT_Bot` مطلقاً ممنوع است.
- **پروتکل راستی‌آزمایی خودکار هاگینگ‌فیس (Self-Healing Verification):** پس از اتمام پوش به هاگینگ‌فیس، اسکریپت `scripts/hf_manager.py` موظف است وضعیت اجرای کانتینر را از طریق API هاگینگ‌فیس بررسی کرده، لاگ‌های سرور را استعلام کند و در صورت وجود ارور استارت‌آپ، آن را پیش از اعلام پایان کار رفع نماید.
- **ممنوعیت رندر رشته‌های تودرتو در onclick:** هیچ متنی از توضیحات دوره‌ها نباید درون صفات اینلاین HTML قرار گیرد؛ داده‌ها باید از آبجکت کش (`window.COURSES_CACHE`) خوانده شوند.
- **ممنوعیت مطلق ایموجی در کنترل‌ها و دکمه‌ها:** هیچ دکمه، پلتفرم (تلگرام، بله، روبیکا، سروش) یا اکشنی نباید با ایموجی‌های متنی (مانند ✏️, ✈️, 📱, 💬) نمایش داده شود. تمام آیکون‌ها باید وکتورهای SVG درون‌خطی (`inline SVG`) مدرن، مینیمال و استاندارد با رنگ متغیر باشند.
- **ممنوعیت هاردکد رنگ‌های خانواده آبی/سرمه‌ای:** استفاده از کلاس‌های ثابت مانند `bg-slate-900`، `bg-[#0c1527]`، `text-slate-400` و حاشیه‌های سرمه‌ای در جداول و کارت‌ها مطلقاً ممنوع است. تمام سطوح باید منحصراً از CSS Variables تم فعال (`var(--panel-bg)`، `var(--card-bg)`، `var(--table-head-bg)`، `var(--border-color)`) تبعیت کنند تا تم‌های Monokai، Catppuccin و دارک خالص ۱۰۰٪ تمیز و بدون لکه آبی اجرا شوند.
- **تایپوگرافی یکپارچه با فونت وزیرمتن (Vazirmatn):** استک فونت سراسری پنل و دیالوگ‌ها باید به فونت وزیرمتن متصل شود (`font-family: 'Vazirmatn', sans-serif`) تا تداخل فونت‌های دندانه‌اره‌ای در ترکیب متون فارسی و انگلیسی به صفر برسد.
- **شمارش واقعی پلتفرم‌های متصل:** کارت آمار داشبورد نباید عدد ثابت ۴ را نشان دهد، بلکه باید صرفاً پلتفرم‌هایی که سشن یا توکن معتبر و تست‌شده دارند را بشمارد.
- **پاکی کامل رشته نسخه:** مقدار نسخه موتور در تمام ماژول‌ها و پیام‌های پینگ منحصراً باید مقدار خالص `v0.3.8` باشد.

---

## 5. تاریخچه نسخه‌ها

| نسخه | تاریخ | خلاصه تغییرات | کامیت |
|:---:|:---:|:---|:---:|
| v0.1.0 | 1403/06/17 | انتشار اولیه امن | 3fae47b |
| v0.1.1 | 1403/06/19 | رفع AUTH_KEY_DUPLICATED، fix_mojibake روبیکا | db850ae |
| v0.1.2 | 1403/06/20 | پاکسازی احراز هویت، اصلاح URL بله | e08b0d4 |
| v0.1.3 | 1403/06/21 | فلو مستقیم فاکتور بله، دکمه C2C، هندلر c2c | fadd3ea |
| v0.1.4 | 1403/06/22 | ایجاد AGENTS.md، قوانین مهندسی دائمی | 37aa8d5 |
| v0.2.0 | 1403/06/23 | بازنویسی AI با Gemini 3.8 و مدل‌های رایگان Nara، اکشن تایپینگ پیوسته | d454e73 |
| v0.2.1 | 1403/06/24 | حذف پاپ‌آپ رمز مرورگر، حذف گروهی و پاکسازی کامل سفارشات، تمیزکاری BALE_OWNER_ID | a089d06 |
| v0.2.2 | 1403/06/25 | تفکیک و رفع باگ تب‌های وب‌پنل، حذف بات رسمی روبیکا ۵۰۲، قفل PID تلگرام و ریسپانسیو موبایل | 9bb9f48 |
| v0.2.3 | 1403/06/26 | معماری ضدگلوله جاوااسکریپت در وب‌پنل، اسکرول‌بار داینامیک، ارتقاء اسنیفر لینک تلگرام، پاکسازی کامل کدهای روبیکا بات | b0bce4d |
| v0.2.4 | 1403/06/27 | بازخورد امنیتی و ارسال آیدی در شکار لینک تلگرام برای غیرادمین، اسکرول‌بار سراسری ۸ پیکسلی با رنگ فالبک | ac3e17a |
| v0.2.5 | 1403/06/28 | اصلاح CDN و قفل وضعیت در اسنیفر تلگرام، تم‌بندی داینامیک اسکرول‌بار، حذف کامل زرین‌پال، تجمیع سکرت‌ها و آپلودر لوگو | 8c1d3aa |
| v0.2.6 | 1403/06/29 | نوار پیشرفت زنده بله، ادغام پرووایدر VyceAI و پالایش تم‌سوئیچر | 61017e7 |
| v0.2.7 | 1403/06/30 | هاب ادمین متقارن، بازیابی مالتی‌پرووایدر AI، قالب‌بندی LTR لاگ‌ها، اسکرابر هدایای دانلودی سایت | 295dbef |
| v0.2.8 | 1403/06/31 | سقف ایمن داینامیک بله با حاشیه امنیت ۱.۵ مگابایت، فشرده‌سازی غیرمسدودکننده، یکپارچه‌سازی رنگ دکمه‌ها با تم، تب اختصاصی فایل‌های دانلودی با صفحه‌بندی | 27a726c |
| v0.2.9 | 1403/07/01 | رفع خطای to_bytes تلگرام، منوی مودال مقاصد هدیه، دراپ‌داون داینامیک مدل‌های هوش مصنوعی، پالایش کامل رنگ صورتی و لاگ تغییر تنظیمات | 9ffaa7b |
| v0.3.0 | 1403/07/02 | هویت یکپارچه دوگانه بله و تلگرام، پایگاه داده رمزنگاری‌شده AES-256، موتور وایرال رفرال، کش file_id و بسته صوتی توحید عملی | dffba25 |
| v0.3.1 | 1403/07/03 | لینک‌های رفرال مبتنی بر شناسه کاربری عددی، ماندگاری دائم دوره‌ها، تعهدنامه پویا، خلاصه‌ساز هوش مصنوعی و درگ‌اند‌دراپ تب‌ها | 92d10df |
| v0.3.2 | 1403/07/04 | یوزرنیم داینامیک بله، دکمه پینگ در بله و تلگرام، خلاصه‌ساز هوش مصنوعی، درگ‌ان‌دراپ لمسی پایدار و فرمت ۳ رقم قیمت | 3b14d7e |
| v0.3.3 | 1403/07/05 | منوی کشویی موبایل (Off-Canvas)، مبدل وکتور SVG به PNG/JPG، هندل پیشوندی استارت رفرال، رفع خطای ادیت دوره و چک‌باکس تگ خواننده | c9a17de |
| v0.3.4 | 1403/07/06 | استودیوی کامل وکتور SVG (تغییر رنگ، متن و خروجی)، ثبت سرفصل دوره از دانلودها، منوی کشویی موبایل و رفع تداخل استارت رفرال | fad2456 |
| v0.3.5 | 1403/07/07 | سایدبار مدرن سمت چپ، حذف کامل تب‌های افقی تکراری، آیکون‌های وکتور خطی و تفکیک کامل ۸ ویوی داشبورد | 85a06c2 |
| v0.3.6 | 1403/07/08 | سایدبار تاشو راست دسکتاپ و موبایل، همگام‌سازی ابری تم، مرتب‌سازی لمسی ناوبری، آکاردئون‌های استودیو و موتور تحویل بسته‌های چندفایله دوره‌ها | acab030 |
| v0.3.7 | 1403/06/27 | رمزنگاری AES-256-GCM سشن‌ها، کلاینت سروش‌پلاس، چک‌لیست چندپلتفرمه ارسال، رفع باگ تحویل فایل دوره، جدول سفارشات ریسپانسیو و درایور لاگ اسلایدی | 396efd2 |
| v0.3.8 | 1403/06/28 | رفع اسکوپ set_system_setting، استخراج واقعی MP4، آیکون‌های وکتور بدون ایموجی، فونت وزیرمتن و شمارنده دقیق پلتفرم‌ها | — |
