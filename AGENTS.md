# UNFINIT Engineering Guidelines
**نگارش جاری موتور: v0.4.8 (Current Engine Version: v0.4.8)**

این سند، قوانین دائمی، خط‌مشی‌های معماری و استانداردهای توسعه پروژه UNFINIT Store Engine را تعیین می‌کند.
کلیه عامل‌ها و مدل‌های هوش مصنوعی موظفند پیش از هرگونه تحلیل یا تغییر در کدها، این منشور را ملاک قطعی عمل قرار دهند.

---

## ۱. چرخه نسخه‌گذاری، گیت و انتشار (Release & Deployment)

### ۱.۱. ارتقاء منظم نسخه و سینک سراسری
- شماره نسخه جاری همواره در ابتدای این سند و در `core/config.py` ثبت است.
- پیش از شروع هر تسک یا پس از پایان کامل تسک و آزمون‌ها، شماره نسخه یک گام Minor ارتقاء می‌یابد.
- نسخه باید در تمام نقاط کلیدی همگام‌سازی شود:
  ۱. درج در ابتدای `AGENTS.md` و جدول نگارش‌ها
  ۲. ثابت `ENGINE_VERSION` در `core/config.py` (شامل مقدار بازگشتی پیش‌فرض و سازنده کلاس)
  ۳. خروجی کلید `engine_version` در متد `get_system_health()` در `services/web_panel.py`
  ۴. بج‌های نسخه در وب‌پنل، استودیو و استورفرانت در `services/web_panel.py` (منحصراً از طریق `config.ENGINE_VERSION`)
- رشته نسخه در تمام لاگ‌ها، پیام‌های پینگ، هدرها و داکیومنت‌ها منحصراً باید فرمت پاک `v0.X.Y` داشته باشد.

### ۱.۲. گردش کار گیت (Git Workflow)
- ثبت کامیت‌ها باید استاندارد و معنادار باشد:
  ```bash
  git add -A
  git commit -m "<type>(<scope>): <clear description>"
  git push origin main
  ```
- پیشوندهای مجاز: `feat`, `fix`, `refactor`, `perf`, `chore`, `docs`

### ۱.۳. همگام‌سازی و سلامت هاگینگ‌فیس (Hugging Face Verification)
- اجرای اسکریپت همگام‌سازی:
  ```bash
  python scripts/hf_manager.py sync
  ```
- **پروتکل خودترمیمی (Self-Healing):** پس از پوش به هاگینگ‌فیس، اسکریپت موظف است وضعیت اجرای کانتینر را از طریق API استعلام کرده، در صورت مشاهده خطای راه‌اندازی، آن را تحلیل و پیش از اعلام موفقیت برطرف سازد. هیچ تسکی بدون تایید بیلد هاگینگ‌فیس خاتمه نمی‌یابد.

---

## ۲. استانداردهای خروجی و گزارش‌دهی (Reporting Standards)

### ۲.۱. ساختار راست‌به‌چپ (RTL Standard)
- کلیه خروجی‌ها، لاگ‌های چت و توضیحات ایجنت باید استاندارد RTL و بدون به‌هم‌ریختگی پرانتزها و علائم نگارشی ارائه شوند.
- واژه‌ها و کدهای انگلیسی باید در بلوک کد یا درون Backtick (`` ` ``) قرار گیرند تا نظم متن حفظ شود.

### ۲.۲. جدول رسمی ارزیابی تغییرات
در پایان هر پاسخ، جدول تغییرات الزامی است:

| ردیف | ماژول / فایل | تغییر اعمال‌شده | وضعیت |
|:---:|:---|:---|:---:|

---

## ۳. اصول امنیت داده و اعتبارسنجی (Security & Auth)

- **حفاظت از سکرت‌ها:** هاردکد کردن توکن، کلید API یا کلمه عبور در سورسکد مطلقاً ممنوع است.
- **اعتبارسنجی سشن وب‌پنل:** تغییر تنظیمات حیاتی (مانند سقف مجاز بله و سکرت‌های هوش مصنوعی) باید صرفاً از طریق سشن معتبر لاگین ادمین انجام شود و نباید خطای مطالبه رمز دستی تولید کند.
- **ایزوله‌سازی فایل‌های حساس:** پوشه‌های سشن (`*.session.enc`)، دیتابیس محلی (`*.db`) و فایل‌های `.env` باید همواره در `.gitignore` باقی بمانند.
- **رمزنگاری سشن‌ها:** کلیه سشن‌های ذخیره‌شده باید با استاندارد AES-256-GCM محافظت شوند.
- **ممنوعیت مطلق هاردکد متادیتای حساب‌ها (Dynamic Metadata Extraction):** اطلاعات هویتی متصل به پلتفرم‌ها (نام، شماره، شناسه کاربر) باید منحصراً از توکن و سشن ورودی استخراج و پارس شوند و هیچ اطلاعاتی نباید در کدهای پایتون به صورت استاتیک یا فرضی نوشته شود.
- **ممنوعیت مطلق واژه VIP و الزام عبارت اشتراک پریمیوم:** استفاده از واژه "VIP" در کلیه متون، عنوان فاکتورها، بج‌ها و دکمه‌ها ممنوع بوده و منحصراً باید عبارت «اشتراک پریمیوم» یا «پریمیوم» به کار رود (به‌ویژه عنوان اینویس بله: `اشتراک پریمیوم ۳۰ روزه`).

---

## ۴. استانداردهای طراحی فرانت‌اند و رابط کاربری (UI / UX)

### ۴.۰. هویت مهندس معمار و طراح ارشد رابط کاربری (Principal UI/UX Designer)
- کلیه تغییرات در کدهای فرانت‌اند باید علاوه بر استحکام معماری و سَندباکس بودن اسکریپت‌ها، از منظر تقارن بصری، تعادل فواصل (Padding / Margin)، هارمونی رنگ‌ها و زیبایی چشم‌نواز بی‌نقص باشند.
- هماهنگی کامل فرم‌ها، دکمه‌ها و آیکون‌ها با استانداردهای مدرن روز و عدم اعوجاج در اندازه‌ها الزامی است.

### ۴.۱. ممنوعیت مطلق ایموجی در کنترل‌ها و دکمه‌ها
- استفاده از ایموجی‌های متنی در دکمه‌ها، سربرگ‌ها، بج‌ها و نام پلتفرم‌ها (مانند ✏️, ✈️, 📱, 💬) مطلقاً ممنوع است.
- تمام آیکون‌ها باید وکتورهای SVG درون‌خطی (`inline SVG`)، مینیمال و هماهنگ با تم باشند.

### ۴.۲. ممنوعیت هاردکد رنگ‌های خانواده آبی/سرمه‌ای
- استفاده از هرگونه رنگ ثابت آبی و سرمه‌ای (`bg-slate-900`, `bg-[#0c1527]`, `bg-[#131b2e]`, `bg-[#1e293b]`, `border-blue-900`) در کل فایل `services/web_panel.py` مطلقاً ممنوع است.
- تمام کارت‌های دوره، کادر وکتور SVG، اینپوت‌های مودال‌ها و نوار ابزار استودیو باید منحصراً از متغیرهای CSS تم فعال پیروی کنند:
  `var(--card-bg)`, `var(--input-bg)`, `var(--panel-bg)`, `var(--table-head-bg)`, `var(--border-color)`, `var(--card-border)`, `var(--accent-color)`
  تا تم‌های Monokai، Catppuccin، Dracula، Vesper و دلبخواه کاربر ۱۰۰٪ یکدست و چشم‌نواز رندر شوند.

### ۴.۳. تایپوگرافی یکپارچه با فونت وزیرمتن (Vazirmatn)
- فونت کل بدنه وب‌پنل، دیالوگ‌ها، سایدبار و المان‌ها باید روی `'Vazirmatn', sans-serif` قفل باشد تا از نمایش فونت‌های دندانه‌اره‌ای در متن دوزبانه جلوگیری شود.

### ۴.۴. شمارش واقعی پلتفرم‌های متصل
- کارت‌های آمار و شمارنده پلتفرم‌های فعال در داشبورد باید بر مبنای سشن و اتصال تست‌شده زنده شمارش شوند، نه اعداد استاتیک فرضی.

### ۴.۵. قانون زبان بصری گوگل متریال ۳ (Google Material 3 Standard)
- کلیه سطوح کارتی و کادرهای وب‌پنل باید از استانداردهای مدرن Material 3 پیروی کنند: سطوح یکدست (`surface-container`)، کنتراست استاندارد متناسب با تم فعال، برچیدن قطعی هرگونه ایموجی متنی از دکمه‌ها، برچسب‌ها و هدرها و جایگزینی انحصاری با وکتورهای SVG خطی و استاندارد.

---

## ۵. معماری نرم‌افزار و تمیزکاری کد (Architecture & Clean Code)

### ۵.۱. معماری متقارن بله و تلگرام (Identical Twin Architecture)
- تمامی سناریوهای کاربری (ثبت‌نام، هویت واحد، کیبوردها، منطق رفرال، سبد خرید، قوانین و تعهدنامه) باید در بله و تلگرام از منطق مشترک (`services/user_service.py` و `services/referral_service.py`) تغذیه شده و رفتاری کاملاً همسان ارائه دهند.

### ۵.۲. معماری ماژولار و سَندباکس جاوااسکریپت (Sandboxed JS)
- کدهای سمت کلاینت در وب‌پنل باید در ۴ ماژول خوداجرای تفکیک‌شده (IIFE) همراه با `try...catch` ایزوله شوند (ناوبری، استودیو، فروشگاه/سفارشات، تنظیمات هوش مصنوعی).
- رویدادهای ناوبری باید از طریق Event Delegation با شناسه `data-tab` مدیریت شوند.
- هیچ متنی از توضیحات یا داده‌ها نباید در صفات اینلاین `onclick` رندر شود؛ تمام مقادیر از کش شیء `window` خوانده می‌شوند.

### ۵.۳. پویایی داده‌ها و ماندگاری ابدی دوره‌ها
- **ممنوعیت هاردکد محتوای تجاری:** هیچ نام دوره یا لینکی نباید هاردکد شود؛ همه باید از دیتابیس استخراج شوند.
- **تضمین پایداری دوره و سفارش:** تابع `init_db` در `core/database.py` تحت هیچ شرایطی نباید دوره‌های ثبت‌شده کاربر در `courses.json` یا جدول `products` را بازنشانی، فیلتر یا حذف کند.
- **شناسه‌های واقعی ربات:** در لینک‌های رفرال و دعوت، شناسه ربات بله باید مستقیماً از خروجی زنده متد `get_me()` استخراج شود و نام‌های فرضی ممنوع است.

### ۵.۴. معماری تک‌کانتینری Drag & Drop
- از تکثیر (Clone) المان‌های DOM تب‌ها برای موبایل اکیداً خودداری شود؛ یک نوار تب واحد با پشتیبانی همزمان رویدادهای Touch و Mouse پیاده‌سازی شود.

### ۵.۵. مستندسازی و کامنت‌های فارسی در کدها (Persian Docstrings & Comments)
- از این پس برای هر ماژول، کلاس یا متد پایتونی و جاوااسکریپتی که ایجاد یا ویرایش می‌شود، باید در خطوط بالایی آن با کامنت استاندارد (`#` یا `/** ... */`) و به زبان فارسی سلیس و ساختاریافته مستندسازی شود:
  ۱. کارکرد، منطق و هدف دقیق آن بخش از کد چیست.
  ۲. چه ورودی‌هایی (پارامترها) دریافت می‌کند و چه نتیجه‌ای (نوع و ساختار خروجی) برمی‌گرداند.
  این قانون الزامی، خوانایی و کیفیت بلندمدت پروژه و سهولت دیباگ را تضمین می‌کند.

### ۵.۶. قانون اکشن‌های بدون رفرش (Zero Page-Reload Principle)
- کلیه عملیات مدیریتی پنل (شامل قطع اتصال و خروج از پلتفرم‌ها، حذف سطرها، تغییر سوئیچ‌ها و ویرایش دوره‌ها) باید منحصراً به صورت غیرهمگام (AJAX/Fetch) انجام شوند. هرگونه رفرش کامل صفحه (`location.reload()` یا سابمیت سنتی فرم) در دکمه‌هایی مانند «قطع اتصال سروش‌پلاس» و «ذخیره تغییرات دوره» ممنوع است؛ المان مربوطه باید به صورت نرم در DOM آپدیت شود.

### ۵.۷. مهار استثناهای شبکه و خطاهای BrokenPipeError
- کلیه جریان‌های ارسال بایت و نوشتن روی خروجی سرور وب (`self.wfile`) باید در برابر خطاهای قطع ارتباط سمت کلاینت (`BrokenPipeError` و `ConnectionResetError`) محافظت شوند تا از خروج سرور جلوگیری گردد. همچنین کلاینت‌های پیام‌رسان نظیر سروش‌پلاس در هنگام تایم‌اوت شبکه موظفند فایل را مستقیماً در صف محلی امن ثبت نمایند.

---

## ۶. تاریخچه نگارش‌ها (Version Changelog)

| نسخه | تاریخ | خلاصه تغییرات کلیدی | کامیت |
|:---:|:---:|:---|:---:|
| v0.1.0 | 1403/06/17 | انتشار اولیه امن پروژه | 3fae47b |
| v0.1.1 | 1403/06/19 | رفع AUTH_KEY_DUPLICATED، fix_mojibake روبیکا | db850ae |
| v0.1.2 | 1403/06/20 | پاکسازی احراز هویت، اصلاح URL بله | e08b0d4 |
| v0.1.3 | 1403/06/21 | فلو مستقیم فاکتور بله، دکمه C2C، هندلر c2c | fadd3ea |
| v0.1.4 | 1403/06/22 | ایجاد AGENTS.md، قوانین مهندسی دائمی | 37aa8d5 |
| v0.2.0 | 1403/06/23 | بازنویسی معماری هوش مصنوعی با Gemini 3.8 و Nara | d454e73 |
| v0.2.1 | 1403/06/24 | حذف پاپ‌آپ رمز مرورگر، حذف گروهی سفارشات | a089d06 |
| v0.2.2 | 1403/06/25 | تفکیک و رفع باگ تب‌ها، قفل PID تلگرام و ریسپانسیو | 9bb9f48 |
| v0.2.3 | 1403/06/26 | معماری ضدگلوله جاوااسکریپت در وب‌پنل، اسکرول‌بار داینامیک | b0bce4d |
| v0.2.4 | 1403/06/27 | اسنیفر لینک تلگرام، اسکرول‌بار سراسری ۸ پیکسلی | ac3e17a |
| v0.2.5 | 1403/06/28 | اصلاح CDN اسنیفر، حذف زرین‌پال، تجمیع سکرت‌ها و آپلودر لوگو | 8c1d3aa |
| v0.2.6 | 1403/06/29 | نوار پیشرفت زنده بله، ادغام پرووایدر VyceAI | 61017e7 |
| v0.2.7 | 1403/06/30 | هاب ادمین متقارن، بازیابی مالتی‌پرووایدر AI، اسکرابر سایت | 295dbef |
| v0.2.8 | 1403/06/31 | سقف ایمن داینامیک بله (۱.۵MB مارجین)، تب اختصاصی دانلودها | 27a726c |
| v0.2.9 | 1403/07/01 | رفع خطای to_bytes تلگرام، منوی مودال مقاصد هدیه، دراپ‌داون AI | 9ffaa7b |
| v0.3.0 | 1403/07/02 | یکپارچه‌سازی هویت بله و تلگرام، رمزنگاری AES-256، رفرال | dffba25 |
| v0.3.1 | 1403/07/03 | رفرال عددی، ماندگاری دائم دوره‌ها، خلاصه‌ساز هوش مصنوعی | 92d10df |
| v0.3.2 | 1403/07/04 | یوزرنیم داینامیک بله، دکمه پینگ، درگ‌ان‌دراپ لمسی پایدار | 3b14d7e |
| v0.3.3 | 1403/07/05 | منوی کشویی موبایل (Off-Canvas)، مبدل SVG به PNG/JPG | c9a17de |
| v0.3.4 | 1403/07/06 | استودیوی کامل وکتور SVG، ثبت سرفصل دوره از دانلودها | fad2456 |
| v0.3.5 | 1403/07/07 | سایدبار مدرن سمت چپ، حذف کامل تب‌های افقی، آیکون‌های وکتور خطی | 85a06c2 |
| v0.3.6 | 1403/07/08 | سایدبار تاشو راست دسکتاپ و موبایل، همگام‌سازی ابری تم، تحویل چندفایله | acab030 |
| v0.3.7 | 1403/06/27 | کلاینت سروش‌پلاس، چک‌لیست چندپلتفرمه ارسال، دراور لاگ اسلایدی | 396efd2 |
| v0.3.8 | 1403/06/28 | رفع اسکوپ set_system_setting، استخراج واقعی MP4، آیکون‌های SVG | 147092f |
| v0.3.9 | 1403/06/29 | تفکیک ۱۰۰٪ تم‌ها در themes.json، پویایی کامل نگارش، احراز هویت سشن GramJS سروش‌پلاس، ماژول فرکانس فراوانی | bfbfd71 |
| v0.4.0 | 1403/06/29 | تزریق CSS Overrides برای تم‌ها، حذف file.splus.ir، استخراج شناسه کاربری سروش، قرینه‌سازی دکمه‌های بله | 685054b |
| v0.4.1 | 1403/06/29 | مستندسازی فارسی، فیکس رنگ‌های متون تم، مودال ویرایش فرکانس، دکمه ارسال سروش‌پلاس و مهار خطای ۴۰۴ | d231fb9 |
| v0.4.2 | 1403/06/29 | ماژول نشانه امروز من، پارس داینامیک سشن سروش‌پلاس، حل ارور StoreService، اکشن‌های بدون رفرش و متریال ۳ | 64e9d6d |
| v0.4.3 | 1403/06/30 | رفع کرش جاوااسکریپت پنل، همگام‌سازی send_audio بله، کش محلی صوت نشانه و مهار خطای پیام منتظر تلگرام | c39f2d9 |
| v0.4.4 | 1403/06/30 | هات‌فیکس بحرانی رفرال، باشگاه پریمیوم VIP با پلن ماهانه، شخصی‌سازی متادیتای نشانه و تاریخ شمسی، بستن مودال‌ها و هویت داینامیک سروش‌پلاس | e95e31d |
| v0.4.5 | 1403/06/31 | هاب سه‌گانه محصولات، اتصال درگاه اینویس بله برای اشتراک VIP، مدیریت کاربران ویژه، ویرایش درجا با دابل‌کلیک، هولد ۴۰۰ms درگ سایدبار و رفع خطاهای اسکوپ بله | ea7201c |
| v0.4.6 | 1403/07/01 | تجمیع تب محصولات با ۳ زیرشاخه، حذف کامل واژه VIP و تبدیل به اشتراک پریمیوم، رفع قطعی کلیک‌های وب‌پنل، مهار BrokenPipeError، صف امن سروش‌پلاس و پروژه‌های ۵‌گانه تحول | a3b4c5d |
| v0.4.7 | 1403/07/02 | دستیار هوشمند کمپرس یا اسپلیت ویدیو، دانلود توربو استریم تلگرام، سوییچ‌های متادیتا و تغییر نام، ساماندهی استودیو و آکاردئون‌ها، درگ‌وان‌دراپ پایدار، نوار ناوبری پایین صفحه موبایل و ارسال دسته‌جمعی رسانه | d1e2f3a |
| v0.4.8 | 1403/07/03 | تفکیک دوگانه محتوای عباس‌منش (نشانه روزانه رایگان و ۱۶ دسته‌بندی پریمیوم)، اسکرپر داینامیک، رفع باگ استریم دانلود تلگرام با فلاش دیسک، انقضای اکشن‌ها و اصلاح ارگونومی نوار ناوبری موبایل | HEAD |
